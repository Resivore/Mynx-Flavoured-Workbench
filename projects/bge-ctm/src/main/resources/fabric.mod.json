{
  "schemaVersion": 1,
  "id": "bge_ctm",
  "version": "${version}",
  "name": "BGE × CTM",
  "description": "Runs Continuity material semantics on canonical BGE appearances and gates them with authoritative physical surface contact.",
  "authors": ["Resivore"],
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "client": ["dev.resivore.bgectm.client.BgeCtmClient"]
  },
  "mixins": [
    "bge_ctm.mixins.json",
    {
      "config": "bge_ctm.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=0.19.3",
    "fabric-api": ">=0.156.0",
    "minecraft": "~26.2",
    "java": ">=25",
    "clutternomore": ">=2.0.7",
    "cnm_terrain_slabs_compat": ">=4.2.22-bge.canary78.stair-wall-surface-authority+26.2",
    "continuity": "=3.0.1+26.2"
  },
  "custom": {
    "runtime_dependency_policy": {
      "contract": "CAPABILITY_OR_PROVIDER",
      "exceptions": [
        {
          "consumer_id": "bge_ctm",
          "relationship": "depends",
          "dependency_id": "minecraft",
          "predicate": "~26.2",
          "reason": "The appearance bridge is compiled against the Minecraft 26.2 block-state and Fabric appearance method descriptors and must remain on the controlled target release.",
          "regression_evidence": [
            "Canary 12 compiles and its focused headless GameTests execute against Minecraft 26.2 and Fabric API 0.156.0+26.2 with exact BGE C78."
          ]
        },
        {
          "consumer_id": "bge_ctm",
          "relationship": "depends",
          "dependency_id": "continuity",
          "predicate": "=3.0.1+26.2",
          "reason": "Canary 12 injects the exact Continuity 3.0.1+26.2 connection-predicate, overlay-processing, quad-context, per-probe contact, SpriteCollector contribution-assembly, processQuadInner emission, and final extra-quad output seams; required mixins and the controlled input hash fail closed rather than guessing across an incompatible implementation.",
          "regression_evidence": [
            "The exact Continuity 3.0.1+26.2 input is SHA-256 controlled, compiles against canonical connection predicates, overlay unit-square processing, quad context, source-full-collision, side/corner probes, SpriteCollector assembly, and processQuadInner emission seams, and is verified as required client mixin targets."
          ]
        }
      ]
    },
    "workbench:classification": "GENERATED / CONTROLLED VALIDATION / RUNTIME UNTESTED",
    "workbench:deployment": "NOT DEPLOYED"
  }
}
