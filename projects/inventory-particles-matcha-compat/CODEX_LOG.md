# Codex Log

## 2026-09-11T06:30:00Z — Create Inventory Particles Matcha Compatibility C1
- Revision: 1
- Source checkpoint: `d69ffc468c59242abcd6ab35e3c7557a2256770a`
- Changes: Audited immutable `InventoryParticles-2.6.0+26.2+fabric.jar` (1,783,277 bytes; SHA-256 `b44d808e673805eea4949591abcb3325faa1abf8b9321266d06533ea5a34c802`; embedded id `inventory_particles`, version `2.6.0+26.2+fabric`). The exact artifact loads every JSON beneath `assets/*/iparticles`, supports exact item and `#tag` holders plus serialized-stack NBT/component predicates, and rebuilds its combined item map on client resource reload. It has no holder precedence or per-stack disable switch: every matching holder spawns. C1 therefore replaces only `dirt_sand.json` and `potion.json`, retaining every upstream holder but excluding the three exact Matcha `minecraft:item_model` values from their poisonous-potato holders, then adds the narrow replacement holders. Its Ribbits definitions use Inventory Particles' item-texture selector, which renders each cited item’s default stack rather than copying art.
- Build/static: Deterministic assembly SHA-256 `7f1c0ff583b3226ce083867b0e16f03c39bebf6a77d2ae5b1e6ca661d0818d6b`; focused archive/routing validator, `git diff --check`, and `python -B tools/workbench.py validate-repository --root .` all passed.
- Runtime: RUNTIME_UNTESTED; no Test Instance Manager transition, deployment, protected 26.1.2 instance access, or runtime claim occurred.
- Artifact: `inventory-particles-matcha-compat-c1.zip`; SHA-256 `7f1c0ff583b3226ce083867b0e16f03c39bebf6a77d2ae5b1e6ca661d0818d6b`.
- Result: ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED.
- Next state: retain the exact generated ZIP, integrate the administrative checkpoint to `main`, publish revision 1 through the main-only Sheet workflow, then perform the focused dedicated-26.2 visual checklist.

## 2026-09-11T16:57:39Z — Diagnose C1 feedback and create Inventory Particles Matcha Compatibility C2
- Revision: 2
- Source checkpoint: `cb3a34b058f2a8fbe8b07a72443ed6de366ce3d1`
- Changes: Recorded the user's external partial C1 RUNTIME_FAIL without inventing results: Green Curry retained poisonous-potato behavior, Glowcap emitted nothing, and the Ribbit Village Explorer Map showed gray/sand-like particles; Ramen and Crystal Heart were not tested and Toadstool Heart had no supplied result. Audited the immutable exact Inventory Particles JAR: `ItemStack.CODEC` serializes modern components below `components`, list-valued `check_value` is valid and checked as alternatives, and `none` succeeds only when no configured node matches. C1 therefore did not fail because of its Matcha component predicate; it missed `dirt_sand_additional_small.json` and `potion_additional_small.json`, whose poisonous-potato holders also accumulate. C2 guards all four exact holders and preserves ordinary potato behavior. C1 also used bare `textures` identifiers, which the parser treats as default ItemStacks, and pooled all three holders; ItemTexture renders only `Item.getDefaultInstance()`. The real Village Explorer Map is `minecraft:filled_map` with `minecraft:custom_data.ribbits:ribbit_village_explorer_map=1`, not a `ribbits:` item. C2 splits the three Ribbits definitions, uses public atlas texture references ending in `.png`, matches that exact map marker, and narrowly patches the family fallback: exact `@glowcap`/`@toadstool_heart` paths are blacklisted and only the marked map is predicate-excluded, leaving unrelated fallback behavior intact.
- Build/static: Deterministic assembly SHA-256 `c694566d81ab0c4c387af1f69501663e3b33d96cad5ea562555a7f0ba7ff2880`; focused C2 archive/routing validator and `git diff --check` passed. C2 validator proves all four upstream potato holders are the only changed members of their files, exact component identities differ from ordinary potato, all Ribbits textures are isolated, the map predicate is exact, fallback narrowing is the only family change, and ZIP entries are exact.
- Runtime: C2 RUNTIME_UNTESTED / NOT_DEPLOYED. No Test Instance Manager transition, protected 26.1.2 instance access, or runtime claim occurred.
- Artifact: `inventory-particles-matcha-compat-c2.zip`; SHA-256 `c694566d81ab0c4c387af1f69501663e3b33d96cad5ea562555a7f0ba7ff2880`. C1 `inventory-particles-matcha-compat-c1.zip` / `7f1c0ff583b3226ce083867b0e16f03c39bebf6a77d2ae5b1e6ca661d0818d6b` remains retained failed historical provenance.
- Result: ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED.
- Next state: retain the exact ignored C2 ZIP in the primary checkout, integrate this revision to `main`, publish revision 2 through the main-only Sheet workflow, then run the focused dedicated-26.2 C2 checklist.

## 2026-09-11T17:20:18Z — Record C2 reload failure and create Inventory Particles Matcha Compatibility C3
- Revision: 3
- Source checkpoint: `46c15de4140b88c58ded0dc7db5315237ac2271f`
- Changes: Recorded the supplied C2 RUNTIME_FAIL / RESOURCE_RELOAD_FAIL exactly: Inventory Particles rejected `inventory_particles:iparticles/mynx/ribbits_village_map.json` and `inventory_particles:ifamilies/fallback/standard.json` with `java.lang.IllegalStateException: Unknown element name:number`; it then reported `Failed to find fallback family config for Inventory Particles!`, failed the Minecraft resource reload, and removed the selected resource packs. C1 had registered 448/448 particle configs and 20/20 families; C2 reached only 449/450 particle configs and 19/20 families before failure, strongly isolating the new C2 resources as the cause. Decompilation of the exact immutable JAR's `NbtNodeType` production codec found only `object`, `string`, `list`, and `int`; `number` is not a codec value. The corresponding production predicate handles its `int` branch as `NumericTag.asInt()`. Ribbits creates the real Village Explorer Map as `minecraft:filled_map` with `minecraft:map_id` and `minecraft:custom_data={ribbits:ribbit_village_explorer_map:1b}` through `CompoundTag.putBoolean`, so its marker is a boolean ByteTag and is safely matched by the exact production `int` branch. C3 changes only the map predicate in the literal config and fallback override from `number` to `int`; all C2 particle routing and fallback narrowing remain otherwise byte-for-byte derived from upstream.
- Build/static: Deterministic assembly SHA-256 `67a8f34f2aff909b4485d63d6da5f1c530dcc76ce23098a77a215773bef33122`; final-ZIP validator hashes the exact immutable JAR, extracts its production enum fields directly from the class files, permits only the discovered node/match values for every final Inventory Particles resource, rejects unknown `this_type` values including `number`, proves the predicate bytecode references `NumericTag.asInt()`, and verifies all C2 routing/fallback boundaries. Focused validator and `git diff --check` passed.
- Runtime: RUNTIME_UNTESTED / NOT_DEPLOYED. No dedicated-26.2 resource reload was performed: both canonical Test Instance Manager experimental slots were already occupied, so C3 did not take or disturb another cohort. No protected 26.1.2 instance access occurred.
- Artifact: `inventory-particles-matcha-compat-c3.zip`; SHA-256 `67a8f34f2aff909b4485d63d6da5f1c530dcc76ce23098a77a215773bef33122`. C1 and the failed C2 artifact/provenance remain retained historical canaries.
- Result: ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED.
- Next state: retain the exact C3 ZIP in the primary checkout, integrate the administrative checkpoint to `main`, publish revision 3 through the main-only Sheet workflow, then execute the reload-safety gate before visual testing.

## 2026-09-11T18:07:41Z — Record C3 runtime result and create Fabric Compatibility C4
- Revision: 4
- Source checkpoint: `63e9b84116c718db92bbeb289933538e75cb28c2`
- Changes: Recorded supplied C3 evidence exactly: the C3 ZIP loaded, Inventory Particles registered 450/450 particle configs and 20/20 families, completed VANILLA and MODDED linking, and completed the resource reload without the C2 parse error; functional behavior still failed as C1 (Green Curry poisonous-potato/default leakage, Glowcap no intended replacement, and Village Explorer Map gray/sand-like behavior). No unreported target is marked PASS. C1-C3 are therefore completed resource-pack investigation provenance, not C4 inputs. Migrated the same UUID and history to `projects/inventory-particles-matcha-compat` without duplicating a canonical project. C4 removes every upstream JSON/fallback overlay and supplies five standalone data-defined replacements. Its three exact, fail-safe client mixins mark only the six C4 spawners, clear markers on config reload, and reject every other `ParticleSpawner` before its predicate/cooldown for an exact target stack; all non-target stacks take the untouched upstream route. Target definitions are proven against current Matcha and Ribbits sources: the three poisonous-potato `item_model` values, exact Glowcap/Toadstool Heart registry IDs, and filled-map + map_id + successful Ribbits custom-data marker.
- Build/static: Clean Java 25 Fabric Loom 1.17.19 build passed. Eight focused JUnit tests passed: real serialized-component ItemStack recognition, ordinary poisonous-potato and normal-filled-map pass-through, unrelated Ribbits identity pass-through, one-holder-per-target dispatch, all target names/resources, exact upstream size/SHA-256, `ParticleHolder.createSpawner`, `ParticlesConfigsManager.reload`, all three `ParticleSpawner` entry points, and all four audited renderer call sites. Final-JAR verification, `git diff --check`, and no-upstream-JSON-overlay checks passed.
- Runtime: C4 is RUNTIME_UNTESTED / NOT_DEPLOYED. No Test Instance Manager transition, dedicated-Workbench mutation, protected 26.1.2 access, or runtime claim occurred.
- Artifact: `inventory-particles-matcha-compat-0.1.0-canary4.jar`; embedded version `0.1.0-canary4`; SHA-256 `e48c4821f8bead5da78205052bf3d1d20db98cccefd5004750a19d54858c1c8b`. Exact audited Inventory Particles validation baseline: `InventoryParticles-2.6.0+26.2+fabric.jar`, 1,783,277 bytes, SHA-256 `b44d808e673805eea4949591abcb3325faa1abf8b9321266d06533ea5a34c802`.
- Result: ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED.
- Next state: retain this exact ignored JAR in the primary checkout, integrate revision 4 to current `main`, validate repository `main`, push, and verify the main-only UUID-keyed Sheet workflow before performing the C4 dedicated-26.2 runtime matrix.
