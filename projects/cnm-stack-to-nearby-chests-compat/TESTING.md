# Testing

## Current gate

**PARKED — NO ACTIVE RUNTIME CANDIDATE**

The frozen standalone CNM × Stack to Nearby Chests project has no source tree,
build, deployable C1 binary, accepted artifact, or rollback artifact. Its C1
designation is historical design evidence only. The project-specific
classification remains **RUNTIME_UNTESTED**; do not inherit the separate
Matcha × Stack to Nearby Chests or Quick Stack Nearby compatibility runtime
results.

The successor-owned evidence is retained without reclassification. Quick Stack
Nearby compatibility C2 routed CNM families at runtime, but the user rejected
its separate-geometry-stack semantics. Exact C3, source
`b52f1858fcc4e0dd1f3dbf79f62378982c2b5c56`, used CNM
`ShapeMap.inSameShapeSet` admission and CNM's native stack-equivalence Mixin so
the existing destination geometry won; the user's complete aggregate report
was **“that's perfect”**. No individual checklist row is inferred. Later C4
and frozen-current accepted C6 received aggregate passes, but their individual
CNM sanity rows were not separately reported. All of that evidence belongs to
Quick Stack Nearby Compatibility UUID
`ffa96cc6-2989-5465-b158-1659a484366b`, not this parked project.

Upstream `stack-to-nearby-chests` 0.5.18, exact SHA-256
`4B35C54B501DD831DEDF82B9AA79F2AC52E87FE856E1C66B98AB79C74C5351B3`,
has separate user runtime evidence for a pre-existing normal-shutdown defect:
its default non-daemon `ForEachContainerTask.TIMER` worker can survive post-main
shutdown. That dependency defect is not a failed-canary result for this absent
adapter and does not upgrade this project's runtime classification.

This migration performed no deployment, Minecraft launch, Test Slot or
accepted-baseline change, dedicated Workbench access, or protected gameplay-
instance access.

## Current useful successor regression

Run this only under separate runtime ownership for the accepted Quick Stack
Nearby compatibility project; it is not authorization to deploy or reactivate
this parked STNC project.

1. Record the exact QSN compatibility, QSN, CNM, BGE, and Nibaru artifact
   filenames, SHA-256 values, source checkpoints, complete enabled stack,
   world, and matching log.
2. Put 20 oak planks in a chest and 16 oak slabs in player storage. Quick Stack
   Nearby should leave 36 oak planks in the destination.
3. Reverse the family direction with slabs already in the destination; the
   result should remain slabs because the existing destination geometry wins.
4. Exercise one CNM Vertical/Step pair and one Nibaru family pair through the
   same exact canonical-family path.
5. Confirm spruce does not merge into oak and that other exact source variants
   remain distinct.
6. Check one Inventory Extended extra-row source plus one lock/keep-count case.
7. Confirm there is no item loss, duplication, ghost stack, relevant log error,
   or unexpected change to ordinary Quick Stack Nearby behavior.

## Stop conditions

Stop and preserve the exact world and log evidence on any source-family
collapse, wrong destination, item conversion, duplication, deletion, ghost
stack, ignored exclusion or locked slot, registry-name-dependent result,
client/server disagreement, crash, or relevant error. Do not promote,
reclassify, or revive the parked project from a successor result, an unreported
row, or a partial result.
