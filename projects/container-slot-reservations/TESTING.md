# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — C24 RUNTIME FAIL RECORDED — C25 RUNTIME UNTESTED**

Current CSR C25 is `container-slot-reservations-0.1.0-canary25.jar`, 223,549 bytes, SHA-256 `3c4660893b38ed0e7b17792a24a31fd068c91ac618d3af0ea5b02c7a57a9a1df`, source `81c3db322703ecc2588fceb3cbd7a1a86fdfb957`, built at `2026-09-18T02:09:24.8825551Z`. Accepted C19 and rollback C4 are unchanged. C25 has no runtime dependency on Easy Shulker Boxes, Item Interactions, Inventory Extended, or Mouse Tweaks; the exact local artifacts are read-only controlled references.

C24 runtime result: **RUNTIME FAIL — occupied-origin RMB drag with a cursor-held shulker still did not perform the requested multi-slot inventory-to-shulker collection.** No individual C24 substage is inferred. The separately confirmed empty-origin carried-shulker-to-inventory direction remains working, and no unrelated result is inferred.

C24's fixed 36-slot client and server eligibility boundary was a proven compatibility defect: exact Inventory Extended 1.1.2 bytecode exposes ordinary physical storage `0..62`, then feet `63`, legs `64`, chest `65`, head `66`, offhand `67`, body `68`, and saddle `69`; InventoryMenu maps ordinary `9..62` to menu `9..62`, hotbar `0..8` to menu `63..71`, and offhand to `72`. The evidence does not identify the failed C24 source row, so fixed-36 is not recorded as the sole cause.

The exact working reference is `EasyShulkerBoxes-v26.2.3-mc26.2.x-Fabric.jar`, 639,115 bytes, SHA-256 `66f803ae8796a0cdd7086e175fe8b96846515710b8b98b9aebc79d52ea2b0c38`, Fabric `easyshulkerboxes` 26.2.3. Its embedded `META-INF/jars/iteminteractions-fabric-26.2.2.jar` is 196,177 bytes, SHA-256 `1745adf294134817986e46bf554a5a6613837602b660e15a15c50bdc2e0a293c`, Fabric `iteminteractions` 26.2.2. Exact bytecode shows Item Interactions registers Puzzles `beforeMouseClick` at `EventPhase.BEFORE`; Puzzles bridges it to Fabric `allowMouseClick` in `puzzleslib:before`, before Fabric default. It reads the carried storage item, resolves the initial and dragged native Slot through `getHoveredSlot(x,y)`, arms on press, traverses in `beforeMouseDrag`, requires `menu.canDragTo`, retains identity sets of every visited draggable Slot for the whole hold, and clears on release. Blank space does not clear visited identity. Its primary action calls the normal `slotClicked`; CSR retains its own server authority instead.

C24's `AbstractContainerScreen.mouseClicked(HEAD)` collector could be unreachable: Fabric's MouseHandler allow callback returns without invoking `Screen.mouseClicked` when Item Interactions interrupts. C25 therefore registers its eligible occupied-origin RMB callback in a CSR Fabric phase ordered before both `puzzleslib:before` and `fabric:default`. It immediately dispatches the origin, uses actual hovered Slot identity for later traversal, and never changes direction. Empty origins pass through. LMB passes through to Easy Shulker Boxes/Item Interactions. Exact Mouse Tweaks 2.31 bytecode also shows its `Main.onMouseDrag` runs before the wrapped Screen drag and can act on an empty entered slot, so one optional `@Pseudo` guard returns false only while CSR owns inbound RMB; ownership survives invalid projection until physical release. No C20-C23 helper/provider bridge or outbound prediction machinery returned.

Client and server eligibility now require the exact live Slot to be active, nonfake, backed by the player's Inventory, still valid, and within `inventory.getNonEquipmentItems().size()`. Creative inventory-tab wrappers are unwrapped to their exact backing Slot; visible menu index and physical inventory coordinate remain distinct. The server continues to verify live menu identity, exact source coordinate and fingerprint, exact count-one supported cursor shulker and predecessor fingerprint, extraction permission, legal planner insertion, callbacks, coherent source/cursor commit, and synchronization. The existing planner still orders compatible occupied stacks, matching reserved empties, then unreserved empties; partial remainders, full no-ops, nesting rejection, and later valid sources are preserved.

Controlled validation used Java 25, Gradle 9.5.1, Loom 1.17.19, and offline `clean test runGameTest build`: 129/129 JUnit/contract tests, 36/36 required headless Fabric GameTests, exact Easy Shulker Boxes/Item Interactions, Mouse Tweaks, and Inventory Extended artifact/bytecode contracts, marker verification, release packaging verification, and final build passed. A separate offline `runGameTest -PincludeInventoryExtendedRuntime` loaded exact Inventory Extended 1.1.2 and passed 36/36 tests, including authoritative collection from physical ordinary slot 62 and rejection of equipment coordinate 63. `git diff --check` passed. These are controlled results, not Minecraft desktop runtime evidence.

This remaining matrix is user-directed desktop runtime validation only. It does not authorize Codex to manage, inspect, launch, modify, or deploy any Minecraft profile or gameplay instance.

1. In the normal modded inventory with Inventory Extended, carry one supported shulker and RMB-press an occupied ordinary slot, then—without releasing—cross occupied cobblestone, logs, an empty slot, bread, dirt, and at least one extended ordinary slot in physical range `36..62`. The origin and each first-seen occupied slot must collect as much as fits into that same cursor-held shulker; the empty slot must do nothing.
2. During the same hold, traverse A→B→A and A→blank→A. The second visit to the same actual Slot must not act again. Release RMB, start a new hold, and confirm A can act again.
3. With Easy Shulker Boxes installed, confirm its configured LMB carried-shulker gesture remains its own single action and CSR's RMB gesture remains one CSR action, with no double transfer. Repeat CSR RMB with Mouse Tweaks 2.31 and MoreMouseTweaks present.
4. Verify compatible occupied internal merge, matching reserved empty, and unreserved empty ordering; mismatching reservations remain unavailable. Confirm a capacity-17 source leaves exactly 47 from 64.
5. With a full shulker and with an illegal nested portable-container source, confirm both sides remain unchanged and a later valid first-seen source in the same hold can still collect.
6. Change the cursor shulker, source contents, menu, or screen mid-hold. The server must fail closed; physical release, screen close, menu change, and disconnect must end collection ownership.
7. Regression: carry a populated shulker, RMB-press an **empty** ordinary player slot, and drag across empty slots. The confirmed outbound shulker-to-inventory behavior must remain unchanged. Ordinary non-shulker cursor, panel, reservation, selection, tooltip, CCAR, and Creative behavior must also remain unchanged.
