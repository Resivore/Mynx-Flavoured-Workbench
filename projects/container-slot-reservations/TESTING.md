# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — C21 RUNTIME FAIL RECORDED — C22 RUNTIME UNTESTED**

Current CSR C22 is `container-slot-reservations-0.1.0-canary22.jar`, 224,946 bytes, SHA-256 `e63cde1a5aa03b7d25382271278a22fbb64dad6fef80d9f9566bc48a69c01cc7`, source `57a4d3a59bb5758df137ef871a4873a2759b4b8b`. It keeps Mouse Tweaks optional and uses the verified exact 75,872-byte Mouse Tweaks 2.31 reference with SHA-256 `4592eff38a2e7af3487688e888a637a8cbd4767b46662df94d993a889102effe`.

Canary 21 is `RUNTIME FAIL` for its sole acceptance goal: **The sole Canary 21 target still failed in gameplay: a carried-stack / ordinary-inventory RMB Mouse Tweaks drag did not successfully deposit items into CSR shulker-panel cells.** Preserve separately only the observed working direction: shulker contents RMB-dragged into empty ordinary inventory slots worked. Do not infer an LMB, shift-LMB, wheel, reservation, or other Mouse Tweaks result.

Exact Mouse Tweaks 2.31 bytecode establishes that its Fabric press callback runs before the screen click, an empty initial slot plus a carried stack can arm RMB, its extended-provider lookup/action runs before the wrapped `Screen.mouseDragged`, and that screen method is still called. C21 incorrectly treated eligibility as proof of provider delivery and suppressed its post-upstream fallback. C22 offers that fallback after upstream processing and shares the existing virtual-cell identity latch with the provider path, so an upstream action and fallback cannot both dispatch the same entered cell. Static evidence cannot distinguish whether the failed C21 runtime missed provider selection, lookup, or action invocation; C22's gated trace now distinguishes those stages.

For a diagnostic run only, add `-Dcontainer_slot_reservations.debugMouseTweaks=true`. The default is off. The 18-stage trace follows press, compatibility/provider selection, initial target and mode, carried stack, provider lookup/resolution, virtual-cell entry, provider action, CSR action, transaction/send, server receive, host/fingerprint and admission, planner, mutation/rejection, and authoritative sync.

The supplied C21 runtime log showed MoreMouseTweaks 2.103.0+26.2 loaded, but that exact artifact was not present under `originals/mods` for inspection. No current evidence attributes the silent no-op to it. Preserve its unrelated behavior and run this focused matrix in the user's normal mod set.

This is user-directed desktop runtime validation only. It neither requires nor authorizes Codex to manage, inspect, launch, alter, or deploy any Minecraft testing profile.

1. Ordinary inventory → shulker — open the CSR panel, put 16 cobblestone on the cursor, RMB-press an empty ordinary inventory slot, keep RMB held, and drag into several empty eligible CSR cells. Each newly entered cell should receive one item, the cursor count should fall by exactly one per accepted cell, repeated movement inside one cell should not duplicate, and the drag should remain continuous across the boundary.
2. Panel-origin deposit — with a carried stack, RMB-press an empty CSR cell and cross additional empty cells. Confirm one deposit per newly entered eligible cell, no duplicate origin replay, correct leave/re-enter behavior, and no client-only host mutation.
3. Latched mode and admission — begin DEPOSIT over an empty initial slot, then cross compatible occupied, unreserved empty, matching reserved empty, mismatching reserved empty, full, non-writable, and forbidden nested-container targets plus an ordinary menu slot. The gesture must remain DEPOSIT until release. Accepted cells consume the exact applicable amount; rejected cells are safe no-ops and do not corrupt the gesture, cursor, projected fingerprint, or total count.
4. Preserve collection direction — RMB-begin over occupied shulker content and drag into empty ordinary inventory slots. Confirm the already observed working direction remains functional and stays COLLECTION_SOURCE even after crossing empty cells. Do not generalize this check to unrelated gestures.
5. Reset, retention, and authority — release RMB and start the opposite initial-slot type; the new press must choose its own mode. The panel may stay open for the single carried-cursor approach bridge and active gesture only, then must resume normal lifetime on release/cancel. Screen close or stale host must clear the latch, and stale menu/locator/fingerprint state must fail closed without loss or duplication.
6. Compatibility regressions — check Mouse Tweaks absent, Mouse Tweaks installed with its RMB tweak disabled, and the normal environment with MoreMouseTweaks present. Preserve single RMB, LMB, shift-LMB, wheel precedence, reservation editing, CCAR header/lock interaction, tooltip, placement, and ordinary fallback behavior. If the target still no-ops, enable the gated trace and stop at the first missing stage rather than inferring a downstream failure.
