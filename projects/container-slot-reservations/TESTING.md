# Testing

## Current gate

**READY TO TEST VERIFIED — RUNTIME UNTESTED — NOT READY FOR PROMOTION**

Current Canary 3 is `artifacts/container-slot-reservations-0.1.0-canary3.jar`, 85,397 bytes, SHA-256 `DFD1A7C1F0E594D12B3FBC1052AE2A1507DD5BD5F5FF9A77D2F8F93667B53EAD`, embedded version `0.1.0-canary3`, from implementation checkpoint `d69469ba90b0d85a257db57ce65588e63f4cc248`. Test Instance Manager checkpoint `3ec1b618829fc99774bffef0ccddfc42bd0a6a44` physically deployed and verified these exact bytes in Slot A as deployment `a9d922c7-346b-4bda-a42a-75b26d64e90e` and artifact `464c5676-979f-4403-93fe-7a2e76d013c5` at manager revision 74, accepted Stack v15, and state digest `5E87E8F64FC1C860FD430DAE15DE3883AB857F12B61E0A09A9DA2C5F712BD8A9`. Minecraft was not launched, so Canary 3 is independently `RUNTIME_UNTESTED`.

Exact user-passed and accepted Canary 1 remains the rollback: `artifacts/container-slot-reservations-0.1.0-canary1.jar`, 63,388 bytes, SHA-256 `4E7F0A470A387BE76189D0A5E1AE8C614D17EC8B24A6774B400838CC234C4532`, source `e0adc6b9302392440f2e5654c1e4031916b0c996`, accepted deployment `c0fdfff1-831a-4081-93a3-e596d7fc928d`, and accepted artifact `c3bc9365-5664-4148-9200-3292307f9bce`. Its exact PASS and accepted Stack provenance are unchanged.

Exact Canary 2 is a failed predecessor, not a rollback: `container-slot-reservations-0.1.0-canary2.jar`, 81,962 bytes, SHA-256 `BB4F269758D43469F2A9C0A36D01DCECB8704054EE8BC01327C46CE614D4A02F`, source `48468ad99ba1353ac5455be39dedef6ca143f7ae`, deployment `bee6248a-0cb8-4953-bd55-33ac3f6e5111`, and artifact `b92b3cd6-a3c2-4882-b279-4a39194fbdc9`. The user reported that its literal zero and other revised reservation presentation were acceptable, but empty-reservation ghost sprites appeared excessively dark, potentially more obviously for naturally dark items. That required visual failure is `RUNTIME_FAIL`; no unobserved checklist row is passed.

Canary 3 passed 46/46 JUnit tests, 10/10 Fabric GameTests, and the complete 13-task clean Java 25 / Gradle 9.5.1 offline build. Exact bytecode checks establish that both ordinary item-atlas and oversized picture-in-picture caches are RGBA8 targets cleared to transparent zero, their final vanilla seams use premultiplied `ONE / ONE_MINUS_SRC_ALPHA` blending, and the vanilla textured shader does not premultiply its input. Canary 3 supplies neutral straight packed `0x59FFFFFF` only for a scoped ghost and uses a custom premultiplied pipeline whose shader attenuates the cached premultiplied RGB and alpha exactly once by `0x59/255`. It never uses `#43362A` as a tint or matte; that value is only the Matcha item-slot background.

The audited blend algebra predicts the same final pixels as Canary 2's packed premultiplied multiplier, so the reported runtime darkness is not explained by the audited contract alone. The focused tests prove the intended input, blend algebra, transparent-pixel behavior, exact upstream seams, and state scoping; they are not visual Minecraft evidence and do not establish that Canary 3 fixes the observed appearance.

The separate report that CSR did not work with Stacks Are Stacks items is deliberately deferred. This task did not investigate, modify, deploy, fix, or test Stacks Are Stacks Container Fixes, and this procedure must not be used to claim otherwise.

## Deployment identity and launch checks

Before every runtime launch, run the Test Instance Manager's read-only verification and require `PHYSICAL_STATE_VERIFIED` for manager revision 74, Stack v15, state digest `5E87E8F64FC1C860FD430DAE15DE3883AB857F12B61E0A09A9DA2C5F712BD8A9`, and the exact Slot A deployment, artifact, filename, hash, and source above. Require QSN C8 to remain the independent verified Slot B member. Stop on any difference; do not redeploy while testing and never access the protected Minecraft 26.1.2 gameplay profile.

Use the exact deployed Stack without enabling, disabling, or replacing any artifact. Optional-absence safety remains controlled evidence unless a separately authorized Test Instance Manager state is created; do not manufacture that configuration during this runtime pass. The deployed tooltip providers must retain the exact audited Easy Shulker Boxes 26.2.3 artifact, 639,115 bytes, SHA-256 `66F803AE8796A0CDD7086E175FE8B96846515710B8B98B9AEBC79D52EA2B0C38`, and its nested Item Interactions 26.2.2 artifact, 196,177 bytes, SHA-256 `1745ADF294134817986E46BF554A5A6613837602B660E15A15C50BDC2E0A293C`. Do not enable Canary 1 or Canary 2 beside Canary 3.

## Runtime matrix

1. Start Minecraft 26.2 with exact Canary 3 and the verified dependency stack. Confirm clean registration, login, supported-container opening, and shutdown without project-attributable classloading, Mixin, shader, rendering, networking, codec, or component errors.
2. On the Matcha `#43362A` item-slot background, inspect several naturally light and naturally dark reserved-item sprites at normal GUI scale and another practical scale. Confirm each retains the original sprite RGB and hue at approximately 35% opacity, with the background naturally visible through it; reject a brown or black tint, dark silhouette, black fringe, baked matte, backing fill, or double attenuation.
3. If the verified test environment already exposes an authorized contrasting diagnostic `#C9E8FF` slot surface, repeat the same light and dark sprites there. Confirm changing only the background changes the visible composite by the expected one-minus-alpha contribution, transparent source pixels remain fully transparent, partially transparent edges remain clean, and no `#43362A` or black precomposition follows the sprite. Do not alter the managed mod Stack or profile to create this surface; otherwise retain the two-background comparison as controlled pixel-algebra evidence only.
4. Exercise both ordinary atlas-rendered items and visually oversized/block-style items that use the picture-in-picture path. Require equivalent alpha-only appearance and original-color preservation on both paths.
5. Inspect ghosts beside ordinary items, the shadowed literal `0`, occupied markers, counts, highlights, text, and tooltips, then repeatedly switch and scroll screens. Confirm `0x59` opacity is scoped to exactly the ghost and never leaks to any later element or frame.
6. Confirm the literal zero remains at vanilla's bottom-right count position with its vanilla-positioned shadow, occupied reservations retain the native item/count plus only the compact marker, the empty hover tooltip is unchanged, and reservation-free empty and occupied slots render exactly normally.
7. Create occupied and cursor-derived empty reservations in a chest, trapped chest, barrel, both physical halves of a double chest, an undyed shulker, and representative dyed shulkers. Close, reopen, save, relog, and restart; confirm exact physical indices, component-rich templates, persistence, and synchronization.
8. Manually insert, QUICK_MOVE, and hopper-insert matching and component-mismatching items. Confirm exact-component admission only, unchanged vanilla merge and destination order, and reservation retention after manual and hopper extraction.
9. Round-trip a carried shulker and inspect its exact physical contents, reservations, other components, `DataComponents.CONTAINER`, fullness, and selection. Confirm no fake zero-count stack, mutation, duplication, deletion, or pre-rendered sprite data was introduced.
10. With the exact audited Easy Shulker Boxes / Item Interactions stack already present in verified Stack v15, use physical shulker indices 0, 8, 9, 17, 18, and 26 to repeat the light/dark, atlas/PIP, literal-zero, occupied-marker, reservation-free, transparent-edge, and state-leakage checks.
11. Confirm optional tooltip dimensions, background, front/back selection highlight, selected-item name, collapse/expand behavior, scrolling, storage bar, colors, fullness, and mouse behavior remain native outside the ghost itself.
12. With two players viewing one supported container, change, replace, and clear reservations from both sides and confirm server-authoritative convergence without stale visuals or unauthorized slot changes. Inspect the complete log and record only rows actually observed for Canary 3.

Stop and record `RUNTIME_FAIL` or `INCONCLUSIVE` if any identity differs, physical readiness fails, either render path is tinted or darkened incorrectly, transparent pixels acquire a fringe or matte, opacity leaks, the zero or marker moves, ordinary or optional rendering changes, state or contents mutate, exact-component admission or vanilla order regresses, persistence or synchronization fails, or a project-attributable error occurs. Do not promote from controlled evidence, do not infer unobserved rows, and do not claim the deferred Stacks Are Stacks issue was tested or fixed.
