# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — RUNTIME UNTESTED**

Current CSR Canary 16 is `container-slot-reservations-0.1.0-canary16.jar`, SHA-256 `34c1f8c5cfca011aca062c55232d2b50ccac2a95f72f8e5a7edec305b5a8c1b2`, source `d16c664e65c61f9b73d2f710e75069f581ec7239`. Accepted C15 remains the independent prior PASS. Use only the dedicated Matcha Flavoured 26.2 Workbench; never open or alter the protected 26.1.2 profile.

With CCAR installed, hover a count-one unnamed unlocked shulker in Survival inventory, Creative inventory, and an ordinary supported container screen. Require exactly one CSR panel, the normal translated shulker name at upper left, and the 10x9 open lock at upper right. Repeat with a named shulker such as `Sack`. Toggle the exact host with CCAR: the normal successful overlay remains and, only after menu synchronization, the badge changes closed/open while the panel, reservations, contents, selection, and interactions remain usable. Test an unusually long custom name: it must truncate before the badge and never enter the grid. Without CCAR, the title remains with no badge or empty gap. Verify bundle tooltips stay unchanged. Stop on any crash, missing/duplicate title or icon, overlap, stale-host panel, unexpected click behavior, lost items, or reservation/routing regression. Record only observed runtime evidence.
