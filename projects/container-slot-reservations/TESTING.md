# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — RUNTIME UNTESTED**

Current CSR C18 is `container-slot-reservations-0.1.0-canary18.jar`, SHA-256 `90d5ebbed363376494a001dcd5235e2d217da1a63f8e4928d870da5be062b1d7`, source `9e74a9363616e8ce0be1de098e788fc08044c8d7`. It pairs with CCAR `0.3.14-csr-panel-lock-canary1`. Test only with explicit Test Instance Manager ownership in the dedicated Matcha Flavoured 26.2 Workbench; never open or alter the protected 26.1.2 profile.

1. Hover a count-one supported shulker in center, top/left, and bottom/right menu positions. Confirm the panel bottom-left originates at the host-slot center, clamps only at the screen edge, keeps its title, contents, ghost reservations, selection, highlights, and marker rendering.
2. Move from host to panel, linger anywhere in the panel, and then leave both host and panel. It must remain visible in the first two cases and disappear immediately in the last; there must be no side rail, invisible corridor, grace frame, or capture-based persistence.
3. Shift+left-click occupied panel cells with normal capacity, a nearly full compatible player stack, and completely full player inventory. Confirm full transfer, exact partial remainder, harmless no-op respectively, exact total counts, preserved source-cell reservation, no routing back into the host, and correct Inventory Extended player-slot coverage if installed.
4. Right-click drag from player inventory into several eligible panel cells, including reserved, unreserved, compatible, and mismatched cases. Drag out as previously supported. Confirm each entered cell acts once while stationary, re-entry is deliberate, multiple/mixed source workflows do not require a fresh click per panel cell, reservations/nesting/capacities remain enforced, and no cursor/count duplication or loss occurs.
5. With CCAR installed, click the header's unlocked sprite, await menu sync, and confirm locked art; repeat to unlock. Verify the click does not affect a panel cell behind it, routing follows the new lock state, the existing keybind is unchanged, ordinary player-inventory hosts work, and any intentionally supported non-player active menu host works. Bundles remain tooltip-only.
6. Recheck ordinary left/right clicks, QSN and CCAR admission, portable-container identity, shulker block/item round trip, and stale host closure. Stop and preserve state on any unexpected panel origin/lifetime, incorrect lock target, lost/duplicated item, reservation regression, crash, or UI regression. Record only actually observed runtime evidence.
