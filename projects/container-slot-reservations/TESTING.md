# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — C23 RUNTIME UNTESTED — C20-C22 SCOPE CORRECTION RECORDED**

Current CSR C23 is `container-slot-reservations-0.1.0-canary23.jar`, 222,985 bytes, SHA-256 `f9267195829174d7555947aae118aba846ace47517e362a3d05ba21b6a119ac5`, source `7dc06e2ceaaa38e863ddbe0dc60a40684ba36638`, built at `2026-09-17T06:19:05.572Z`. Mouse Tweaks remains optional. The exact local Mouse Tweaks 2.31 inspection target is 75,872 bytes with SHA-256 `4592eff38a2e7af3487688e888a637a8cbd4767b46662df94d993a889102effe`; it is test-only and is neither bundled nor a runtime dependency.

Canonical correction: C20-C22 misread the requested reverse gesture as an ordinary item carried by the cursor being RMB-dragged into virtual CSR panel cells. The requested interaction has always been one supported shulker carried by the cursor, RMB-dragged across occupied ordinary player-inventory slots so those whole source stacks are collected into that same shulker. User runtime evidence establishes that this actual inventory-to-carried-shulker direction remained nonfunctional through C22. The opposite carried-shulker-to-empty-inventory direction is confirmed working and is C23's preservation baseline. Historical C20-C22 records remain intact.

C23 removes the virtual Mouse Tweaks provider, ordinary-cursor panel projections and shadows, special panel retention/native-boundary workarounds, `SECONDARY_DEPOSIT`, obsolete menu-source payload, and obsolete diagnostics. It uses one count-one cursor-shulker gesture with `SHULKER_TO_INVENTORY` and `INVENTORY_TO_SHULKER` modes. The initial ordinary slot latches direction until release. Mouse Tweaks' exact native slot-entry, origin-replay, same-slot de-duplication, A-to-B-to-A re-entry, and blank-boundary re-entry semantics drive the ordinary inventory targets. Inbound actions project per-source remainders and a carried-shulker predecessor chain, while the server independently validates the current menu, exact visible/physical source slot, source fingerprint, and cursor-shulker fingerprint before planning and committing both sides.

Controlled validation used Java 25, Gradle 9.5.1, Loom 1.17.19, and offline `clean test runGameTest build`. All 108 JUnit/contract tests and all 35 required headless Fabric GameTests passed, including exact Mouse Tweaks 2.31 bytecode/packaging contracts and release/marker checks. These are controlled results, not Minecraft desktop runtime evidence.

This remaining matrix is user-directed desktop runtime validation only. It does not authorize Codex to manage, inspect, launch, modify, or deploy any Minecraft profile or gameplay instance.

1. **Outbound baseline:** carry a populated shulker, RMB-press an empty ordinary player slot, and drag across several empty slots. Each entered slot should receive the selected shulker content exactly as before; the shulker stays on the cursor.
2. **Inbound multiple types:** carry a shulker, RMB-press an occupied slot, then cross occupied cobblestone, logs, bread, and dirt slots. The initial and each newly entered stack should move as far as the whole-shulker planner permits into the same cursor-held shulker.
3. **Mixed grid and latch:** begin inbound on occupied, then cross occupied, empty, occupied, empty, occupied. Empty entries are no-ops and direction never changes. Separately begin outbound on empty and cross occupied; it must not reverse into collection.
4. **Reservations and planner order:** use compatible occupied internal stacks, matching reserved empties, mismatching reserved empties, and unreserved empties. Confirm merge-first, matching-reservation-second, unreserved-empty-third behavior; a mismatching reservation must not block another valid destination.
5. **Partial capacity:** with a source stack of 64 and capacity for only 17, confirm 17 enter the shulker and exactly 47 remain in the source slot.
6. **Full shulker continuation:** a source with no available destination remains unchanged, and the same held gesture can still process a later valid source.
7. **Nesting rejection continuation:** crossing a shulker or other forbidden portable container is a no-op, without ending the gesture or blocking a later valid source.
8. **Entry identity:** repeated callbacks inside one slot act once. A-to-B-to-A and A-to-blank-to-A each permit the later genuine re-entry exactly once.
9. **Release and opposite mode:** releasing RMB ends the latch. A new press on the opposite slot type chooses the opposite direction.
10. **Fail-closed authority:** stale menu, changed source, changed/non-count-one cursor shulker, or divergent fingerprint must mutate neither side and must not duplicate or lose items.
11. **Ordinary cursor:** carry a non-shulker item and use normal Mouse Tweaks near ordinary slots and the CSR panel. No CSR special drag or ordinary-item-to-virtual-panel bridge should activate; normal vanilla/Mouse Tweaks semantics and immediate panel lifetime remain.
12. **Regression surface:** verify panel LMB/RMB, shift-click, reservation editing, selection, tooltip, CCAR header/lock integration, host-centered lifetime, Creative synchronization, Mouse Tweaks absent/disabled behavior, and the user's normal environment with MoreMouseTweaks present.
