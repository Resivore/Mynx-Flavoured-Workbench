# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — RUNTIME UNTESTED**

Current CSR C20 is `container-slot-reservations-0.1.0-canary20.jar`, 214,374 bytes, SHA-256 `2da8356af2fa878f25bb1de424b87fd26e73d3a236ba7e52acef4f8a8249bdaa`, source `ad41843e1095b77427bd4debfbc71d396b320b39`. It targets the locally inspected `MouseTweaks-fabric-mc26.2-2.31.jar`, SHA-256 `4592eff38a2e7af3487688e888a637a8cbd4767b46662df94d993a889102effe`. It pairs with CCAR `0.3.14-csr-panel-lock-canary1`. Test only with explicit Test Instance Manager ownership in the dedicated Matcha Flavoured 26.2 Workbench; never open or alter the protected 26.1.2 profile.

1. RMB drag — with Mouse Tweaks 2.31 enabled, carry a stack from the actual host Slot into multiple panel cells, including a revisit. Confirm one placement per eligible entered cell, matching reservations accept, mismatches/full cells reject, no duplicate/lost count, and a host-to-panel then panel-to-player transition stays one gesture. Disable Mouse Tweaks' RMB tweak and confirm CSR's existing once-per-entered-cell drag remains.
2. LMB drag — collect/move compatible stacks among panel cells, begin on a panel cell and continue to ordinary player slots, then begin on the open host's ordinary menu Slot and continue into the panel. Confirm compatible stacks move normally, incompatible/reserved-rejected cells do not mutate, and no native host/underlay drag leaks through covered panel coordinates.
3. Shift-LMB drag / quick move — exercise panel → player inventory and player inventory → panel, including the same host/panel boundary transition. Verify partial remainders, reservation admission, effective capacities, nesting rejection, and conservation.
4. Wheel — over a panel cell, use each Mouse Tweaks wheel-transfer direction that applies between the virtual shulker region and player inventory; it must transfer one item per configured step. When Mouse Tweaks does not consume the scroll (disabled, empty, or unavailable transfer), CSR's occupied-cell selection cycling must still occur.
5. Regression — repeat normal single LMB/RMB panel actions, existing CSR shift-click, reservation editing, CCAR lock-header click, tooltip, host-centered viewport clamping, immediate host-or-panel open/close, Creative cursor synchronization, stale-host/fingerprint rejection, and the same tests with Mouse Tweaks absent. Stop on any disappearance, duplication, server/client disagreement, stale-host mutation, or gesture leak.
