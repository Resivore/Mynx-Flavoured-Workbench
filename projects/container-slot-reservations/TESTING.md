# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — RUNTIME UNTESTED**

Current CSR Canary 17 is `container-slot-reservations-0.1.0-canary17.jar`, SHA-256 `5ae4d65b70b8f8d2ed07d9b33724ae9a1b7048518bb5a4ba6ef72fadbf0f4b82`, source `34f0104c561ff93c12999fc7b14a57d3d8643114`. Accepted C15 remains the independent prior PASS. Use only the dedicated Matcha Flavoured 26.2 Workbench; never open or alter the protected 26.1.2 profile.

In a supported storage slot, reserve a non-empty count-one shulker, remove it, change contents (including fully emptying it), rename it, recolor it, and return it: each return must be admitted. A separate lookalike shulker with the same contents must be denied. Repeat the changed-contents and lookalike cases for a bundle; recolor it too if the normal 26.2 dye recipe is available. Place and break the identified shulker, then confirm its recovered item still returns to the reserved slot. Reserve anonymous empty shulkers and bundles (including an empty stack where native rules permit): no visible behavior or stacking change should occur, and equivalent anonymous empty containers remain eligible. Check ordinary renamed/component-distinct items remain component-exact. Recheck the existing panel title, header badge with/without CCAR, selection, transfer ordering, and QSN/CCAR admission. Stop on any unexpected admission/denial, lost item, crash, stale panel, changed empty-container stacking, or UI regression. Record only observed runtime evidence.
