# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — RUNTIME UNTESTED**

Current CSR C21 is `container-slot-reservations-0.1.0-canary21.jar`, 220,172 bytes, SHA-256 `8eeab0e1ed2c6c6c4747615b471fb78a8c708c9455b2c8798709a394ab4cc13c`, source `b137377b6f54fd7e5cc6d621f815d09187a30912`. It keeps Mouse Tweaks optional and uses the inspected 2.31 reference SHA-256 `4592eff38a2e7af3487688e888a637a8cbd4767b46662df94d993a889102effe`.

Historical C20 external evidence is narrow: shulker contents RMB-dragged into empty ordinary inventory slots worked (PASS limited to that direction); inventory/cursor RMB-dragged into empty CSR shulker-panel cells did not work (FAIL limited to that direction). It is not C21 runtime evidence and does not imply results for LMB, shift-drag, wheel, reservations, or any other row.

This is user-directed desktop runtime validation only. It neither requires nor authorizes Codex to manage, inspect, launch, alter, or deploy any Minecraft testing profile.

1. Deposit into shulker — put 16 compatible items on the cursor, RMB-press an empty CSR panel cell, keep RMB held, and cross several empty cells. Confirm C21 latches DEPOSIT at RMB-down, adds one item to each newly entered eligible cell, does not repeat on a stationary cell, and permits the documented Mouse Tweaks 2.31 re-entry behavior after leaving and returning.
2. Admission and conservation — repeat across matching reserved cells, mismatching reservations, a partial compatible stack, full cells, non-writable cells, and a portable nested-container attempt. Matching/compatible targets may accept; every rejected target must be a safe no-op that does not corrupt the continuing gesture, host fingerprint, cursor, or total count. Cross a mixture of occupied and empty cells and confirm DEPOSIT never flips to collection.
3. Cross-boundary deposit — with the panel already open and Mouse Tweaks RMB tweak enabled, start over an ordinary empty inventory destination while carrying a stack and drag into panel cells; then start on an empty panel cell with a carried stack and drag into ordinary empty inventory slots. Confirm the panel remains available for the carried/latched RHS bridge, ordinary-slot behavior remains Mouse Tweaks-owned where applicable, and no duplicate/lost count occurs.
4. Collection regression — RMB-press an occupied panel cell and drag into several empty ordinary inventory slots. Confirm C20's observed shulker-to-inventory behavior remains functional. Cross empty and occupied cells without changing the press-time COLLECTION_SOURCE mode.
5. Reset and authority — release RMB, then begin the opposite initial-slot type and confirm the new press chooses its own mode. Close the panel/screen or make the host stale and confirm the latch clears and the next virtual action fails closed. Confirm a native cross-boundary action only resumes a projected CSR chain after its cursor state converges.
6. Regression sanity — check single RMB, LMB, shift-LMB, wheel precedence, reservation editing, CCAR lock/header interaction, tooltip, panel lifetime/placement, Mouse Tweaks absent, and Mouse Tweaks RMB tweak disabled. Stop on any disappearance, duplication, server/client disagreement, stale-host mutation, or gesture leak.
