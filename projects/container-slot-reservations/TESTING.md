# Testing

## Current gate

**READY TO TEST VERIFIED — RUNTIME UNTESTED — NOT READY FOR PROMOTION**

Current Canary 2 is `artifacts/container-slot-reservations-0.1.0-canary2.jar`, 81,962 bytes, SHA-256 `BB4F269758D43469F2A9C0A36D01DCECB8704054EE8BC01327C46CE614D4A02F`, embedded version `0.1.0-canary2`, from implementation checkpoint `48468ad99ba1353ac5455be39dedef6ca143f7ae`. Test Instance Manager checkpoint `2b3d93b2df3d2f2bd5c851570c9ca191c85bafb4` physically deployed and verified these exact bytes in Slot A as deployment `bee6248a-0cb8-4953-bd55-33ac3f6e5111` and artifact `b92b3cd6-a3c2-4882-b279-4a39194fbdc9` at manager revision 71, accepted Stack v15, and state digest `BF3B0CAFA400620A9755F9D2CD3A3080EB726691922553DA3C945F76A25E86F9`. Minecraft has not been launched and Canary 2 has no runtime result.

Exact user-passed and accepted Canary 1 remains the rollback: `artifacts/container-slot-reservations-0.1.0-canary1.jar`, 63,388 bytes, SHA-256 `4E7F0A470A387BE76189D0A5E1AE8C614D17EC8B24A6774B400838CC234C4532`, source `e0adc6b9302392440f2e5654c1e4031916b0c996`, accepted deployment `c0fdfff1-831a-4081-93a3-e596d7fc928d`, and accepted artifact `c3bc9365-5664-4148-9200-3292307f9bce`. Its exact PASS and accepted Stack provenance are unchanged; do not rebuild, relabel, or modify it.

Canary 2 passed a clean Java 25 / Gradle 9.5.1 `test runGameTest build` sequence: 41/41 JUnit tests and all 10 required Fabric GameTests passed. The static suite validates the exact Minecraft 26.2 premultiplied item-rendering seams, component-rich reservation identity, slot indices 0 through 26, no-mutation and no-reservation paths, and optional pseudo-mixin handler/linkage contracts. These are controlled build results, not gameplay evidence. No client or gameplay profile was launched.

The optional tooltip audit inputs were read-only exact Easy Shulker Boxes `26.2.3`, `EasyShulkerBoxes-v26.2.3-mc26.2.x-Fabric.jar`, 639,115 bytes, SHA-256 `66F803AE8796A0CDD7086E175FE8B96846515710B8B98B9AEBC79D52EA2B0C38`, and its nested Item Interactions `26.2.2`, `META-INF/jars/iteminteractions-fabric-26.2.2.jar`, 196,177 bytes, SHA-256 `1745ADF294134817986E46BF554A5A6613837602B660E15A15C50BDC2E0A293C`. Runtime metadata keeps both integrations optional with flexible `*` predicates; the exact hashes are validation identities, not runtime ceilings.

## Deployment identity and launch checks

Before every runtime launch, run the Test Instance Manager's read-only verification and require `PHYSICAL_STATE_VERIFIED` for exact revision 71, Stack v15, state digest `BF3B0CAFA400620A9755F9D2CD3A3080EB726691922553DA3C945F76A25E86F9`, and the Slot A deployment/artifact/file/hash/source identity above. Stop if any value differs. Do not reassign either slot or modify the dedicated profile while testing; preserve the independent Slot B state and never touch the protected Minecraft 26.1.2 gameplay profile.

Test first without Easy Shulker Boxes and Item Interactions to prove the optional integration is absent-safe. For the tooltip cases, use the exact audited Easy Shulker Boxes artifact and its required compatible providers, and verify their resolved identities before launch. Do not enable the disabled Canary 1 predecessor or otherwise load Canary 1 and Canary 2 together.

## Runtime matrix

1. Start Minecraft 26.2 with Fabric Loader 0.19.3 or newer, Fabric API 0.157.0+26.2, and exact Canary 2 without Easy Shulker Boxes or Item Interactions. Confirm clean registration, login, inventory/container opening, and shutdown without project-attributable classloading, Mixin, networking, rendering, codec, or component errors.
2. In a chest, trapped chest, barrel, both physical halves of a double chest, an undyed shulker, and representative dyed shulkers, create occupied and cursor-derived empty reservations. Close and reopen each container and confirm exact physical indices and component-rich templates persist.
3. Inspect empty reserved slots at normal GUI scale and at another practical scale. Confirm the actual item sprite is approximately 35% opaque, a normal shadowed literal `0` occupies vanilla's bottom-right count position, and no cyan outline, white wash, or cyan wash remains. Confirm the existing empty-reservation hover tooltip is unchanged.
4. Place physical stacks into reserved slots and confirm the native item/count remains intact with only the compact occupied marker. Verify unreserved empty and occupied slots remain completely native.
5. Inspect several reserved ghosts beside ordinary items, counts, text, and tooltips, then repeatedly open, scroll, and switch screens. Confirm opacity is limited to each ghost sprite and never leaks into a later item, zero, marker, tooltip, highlight, or screen element; include a visually oversized/block-style item that uses the picture-in-picture path.
6. Reserve two registry-equal but component-distinct stacks and confirm their sprites, names, models, and admission behavior stay distinct. Manually insert, QUICK_MOVE, and hopper-insert matching and mismatching stacks; confirm only exact component matches enter reserved empty slots and vanilla merge/destination order otherwise remains unchanged.
7. Extract matching stacks manually and by hopper until the slots are empty, then save/reload and round-trip a carried shulker. Confirm reservations remain, unrelated components and physical contents are unchanged, and no item is duplicated, deleted, or rewritten.
8. Enable the exact audited Easy Shulker Boxes stack. Open vanilla-shulker contents tooltips containing reservations at physical indices 0, 8, 9, 17, 18, and 26. Confirm every empty reservation renders the same approximately 35%-opaque component-exact ghost plus literal `0` at its exact grid slot.
9. In the same tooltip, confirm occupied reserved slots retain their native stack/count plus the compact marker, unreserved empty slots remain blank, and occupied unreserved slots remain native. Repeat while moving the selected slot across reserved and unreserved cases.
10. Confirm tooltip dimensions, background, selected-slot back/front highlight, selected-item name, collapse/expand behavior, scrolling, storage bar, colors, fullness, and mouse interactions remain native. Direct insertion and extraction controls need not honor reservations in this Canary.
11. Compare a reservation-free shulker tooltip before and after enabling Canary 2. Confirm it is visually and behaviorally unchanged. After tooltip use, inspect the shulker's exact `DataComponents.CONTAINER`, reservations, other components, physical item counts, selection, and fullness to confirm no mutation or fake item was introduced.
12. With two players viewing the same supported placed container, change, replace, and clear reservations from both sides and confirm server-authoritative convergence without stale visuals or unauthorized slot changes.
13. Save, relog, restart cleanly, and inspect the complete log for project-attributable errors. Preserve the exact Canary 1 rollback and record only observations actually made for Canary 2.

Stop and record `RUNTIME_FAIL` or `INCONCLUSIVE` if any identity differs, deployment/readiness verification fails, optional-mod absence causes loading failure, opacity is simulated or leaks, the literal zero or physical index is wrong, an empty outline/wash remains, a native tooltip feature changes, physical contents/reservations/components/fullness/selection mutate, an unsupported slot becomes eligible, exact-component admission or vanilla ordering regresses, persistence/synchronization fails, or a project-attributable error occurs. Do not infer unobserved rows from a launch-only result.
