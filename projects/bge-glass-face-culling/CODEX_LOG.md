# Codex Log

## 2026-09-19T04:31:50.1050629Z — Build Canary 1 geometry-aware glass face culling

- Revision: 1
- Source checkpoint: `dd70711d2c872d512fa63e86289185f2a5546aea`
- Changes: Added a client-only final-phase baked-model wrapper that receives upstream transformed Renderer API quads, reconciles the vanilla boolean neighbor-culling result only for mutually compatible canonical BGE glass bindings, and subtracts exact coplanar receiver patches supplied by BGE C78. Deterministic grid partition preserves every nonoverlapping rectangle; fragment emission copies quad metadata and bilinearly interpolates position-dependent UV, color, lightmap, and normal data while preserving winding. Unsupported or non-grid quads pass through unchanged. Material eligibility is limited to BGE `GLASS_EDGE` profiles with `TRANSLUCENT_ADJACENCY`, and mutual compatibility requires the same canonical material identity. Exact BGE C78 remains unchanged as the sole geometry authority; `projects/bge-ctm`, `projects/translucent-glass-bge-compat`, `WORKBENCH_SERVER_STATE.json`, and unrelated projects remain unchanged, with no CTM, shader, resource-pack, collision, placement, or BGE geometry-generation ownership claimed.
- Build/static: A Java 25 clean `gradlew clean check stageCanaryArtifact` passed. Ten unit tests cover exact rectangle subtraction, bilinear interpolation, full and partial quad-fragment emission, and untouched disjoint geometry. Seven BGE-backed Minecraft 26.2 GameTests cover canonical material compatibility, full and compound partial contact, all 40 Stair states, all 161 nonempty Wall states, LOW/TALL/post topology, and overlap/subtraction. Exact BGE C78 and provider hashes, the public Fabric model-loading and final-quad consumer seams, packaged metadata, and production JAR contents passed verification. These are controlled checks, not Minecraft visual-runtime evidence.
- Runtime: No Minecraft gameplay/testing profile was launched, inspected, selected, or modified. No client visual, Continuity-stack, or Complementary-shader observation was supplied; Canary 1 is `RUNTIME_UNTESTED`.
- Artifact: Canary 1 is `bge-glass-face-culling-0.1.0-canary1.jar`, 26,988 bytes, SHA-256 `00e1e909a36c7575e42163e08b33eb7982f9dc051c753bdfceedda2fe8d1e640`, finalized at `2026-09-19T04:30:03.6696080Z` from the preceding coherent implementation checkpoint `dd70711d2c872d512fa63e86289185f2a5546aea`.
- Result: `ACTIVE / CONTROLLED_VALIDATION_PASS / RUNTIME_UNTESTED` — exact Canary 1 bytes are the current controlled-validated release, with no acceptance, rollback, or blocker recorded.
- Next state: Under explicit runtime ownership, execute `TESTING.md` against the exact Canary and BGE hashes with shaders off, the supported Continuity stack, and Complementary enabled; record only the rows actually observed.

## 2026-09-19T05:18:52.5549747Z — Build Canary 2 null-direction and final-wrapper correction

- Revision: 2
- Source checkpoint: `437b3dce25218eb901e26b662d021e2ad4e2239b`
- Changes: Corrected C1's culling predicate so a null `Direction` delegates exactly to the upstream predicate without `pos.relative(...)`, neighbor lookup, or BGE surface-overlap evaluation. Real Directions retain the C1 compatible-BGE boundary extension. Added three focused unit regressions for null upstream-true, null upstream-false/no boundary read, and ordinary non-null upstream culling. The Fabric after-bake phase now follows `ModelModifier.WRAP_LAST_PHASE`, placing the glass wrapper around Slab Decorations' late wrapper so clipping receives its emitted transformed geometry; C2 remains a BGE C78-only, fail-open consumer with no CTM, provider, geometry-reconstruction, or material-policy expansion. `projects/slab-decorations`, BGE, BGE × CTM, `WORKBENCH_SERVER_STATE.json`, and unrelated projects remain unchanged by this release.
- Build/static: A Java 25 clean `check stageCanaryArtifact` passed. Thirteen unit tests passed, including all three null-direction regressions; seven Minecraft 26.2 BGE-backed GameTests passed, including every C1 rectangle subtraction, interpolation/emission, canonical material, Stair (all 40), Wall (all 161 nonempty), Layer, Slab, Step, and overlap proof. Exact BGE C78 and Fabric provider hashes, the C2 model-wrapper/renderer seam, packaged metadata, and production JAR contents passed controlled verification. These are controlled checks, not Minecraft client runtime validation.
- Runtime: The owner supplied exact C1 runtime evidence: it crashed the previously working world during chunk mesh construction with a null Direction in `GlassCullingBlockStateModel`, and disabling C1 let that same world open. C2 itself has not been run in a real Minecraft client, so C2 remains `RUNTIME_UNTESTED`; `TESTING.md` requires opening that world and allowing nearby meshes to build/rebuild before the visual matrix. No testing profile was inspected, selected, or modified.
- Artifact: Canary 2 is `bge-glass-face-culling-0.1.0-canary2.jar`, 27,070 bytes, SHA-256 `51de571fd2bc549f02fa415b8189f4acf62fb1851051417f5cd7cb011b18914c`, finalized at `2026-09-19T05:18:27.8291128Z` from the preceding coherent implementation checkpoint `437b3dce25218eb901e26b662d021e2ad4e2239b`.
- Result: `ACTIVE / CONTROLLED_VALIDATION_PASS / RUNTIME_UNTESTED` — C1 is confirmed runtime-failing on the owner-reported null-Direction path; exact C2 bytes are controlled-validated but await owner runtime verification, with no acceptance, rollback, or blocker recorded.
- Next state: Under explicit runtime ownership, execute C2's first regression gate and the remaining `TESTING.md` matrix against the exact Canary 2 and BGE C78 hashes, recording only actually observed results.

## 2026-09-19T06:39:55.7715436Z — Build Canary 3 full-glass partial-overlap correction

- Revision: 3
- Source checkpoint: `a559613c1b5efba1118bb370d232be7cc1519276`
- Changes: Corrected C2's directional cull ownership: C2 evaluated `upstreamWholeFaceCull || canEvaluateBoundary`, so an eligible full-glass ↔ partial-BGE boundary could be discarded whole before `GlassQuadClipper` received it. C3 preserves C2's exact null-direction delegation, but for a real compatible BGE-evaluable boundary returns `false` before the upstream whole-face predicate. The complete source face then reaches the existing exact clipper, which emits nothing for full overlap, only exposed fragments for partial overlap, and the complete source face for zero overlap. Noneligible boundaries retain upstream behavior. No quad `cullFace` metadata, BGE model-generation policy, `SurfaceOverlapResolver`, material compatibility, CTM policy, BGE, BGE × CTM, Continuity, Slab Decorations, `WORKBENCH_SERVER_STATE.json`, or unrelated project changed.
- Build/static: A Java 25 clean `gradlew clean check stageCanaryArtifact` passed from the source checkpoint, followed by a forced standard artifact-stage verification. Sixteen unit tests passed, including null delegation, noneligible directional preservation, eligible whole-face-cull bypass, and predicate-to-clipper partial-fragment handoff. Eight Minecraft 26.2 BGE-backed GameTests passed: full/full complete removal, full/partial exact complement for bottom/top Slab, Vertical Slab, Step, Layer, Quarter Column, Corner, Stair, and Wall, partial/partial intersection, noncontacting compatible partial preservation, all 40 Stair states, and all 161 nonempty Wall states. Exact BGE C78 and Fabric provider hashes, public rendering seams, packaged metadata, and client-only production JAR boundary passed verification. These are controlled checks, not Minecraft visual-runtime evidence.
- Runtime: The owner supplied C2 observations only: the previously affected world opened without the C1 null-`Direction` chunk-meshing crash; partial BGE glass geometry ↔ partial BGE glass geometry appeared correct; and full glass ↔ partial BGE glass incorrectly removed the full touching face when only a partial coplanar overlap existed. C3 was not launched or inspected in a real Minecraft client, so it remains `RUNTIME_UNTESTED`. No Minecraft testing profile was inspected, selected, populated, cleared, or modified.
- Artifact: Canary 3 is `bge-glass-face-culling-0.1.0-canary3.jar`, 27,257 bytes, SHA-256 `df55e0c384292c7c79be19be0a6ac4c6b7ce373cb0c6f4de63413817f1ea8d2e`, finalized at `2026-09-19T06:39:44.6852639Z` from the preceding coherent implementation checkpoint `a559613c1b5efba1118bb370d232be7cc1519276`.
- Result: `ACTIVE / CONTROLLED_VALIDATION_PASS / RUNTIME_UNTESTED` — exact Canary 3 bytes are current and controlled-validated. No acceptance, rollback, or blocker is recorded.
- Next state: Under explicit runtime ownership, open the affected world with the exact C3 artifact; verify the former full ↔ partial failure now removes only actual overlap; recheck representative partial ↔ partial contacts; then perform the documented Continuity and Complementary matrix. Record only observed C3 client behavior.
