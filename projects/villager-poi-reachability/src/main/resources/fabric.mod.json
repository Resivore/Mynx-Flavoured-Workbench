{
  "schemaVersion": 1,
  "id": "villager_poi_reachability",
  "version": "${version}",
  "name": "Villager POI Reachability",
  "description": "Narrow raised job-site reachability compatibility for villagers.",
  "authors": ["Resivore"],
  "license": "All-Rights-Reserved",
  "environment": "*",
  "mixins": ["villager_poi_reachability.mixins.json"],
  "depends": {
    "minecraft": "=26.2",
    "fabricloader": ">=0.19.3",
    "fabric-api": ">=0.157.0",
    "java": ">=25"
  },
  "custom": { "runtime_dependency_policy": { "contract": "CAPABILITY_OR_PROVIDER", "exceptions": [] } }
}
