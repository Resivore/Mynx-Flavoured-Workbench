# Testing

## Canary 19 — pending dedicated-slot runtime test

Canary 18 externally failed client startup: its Mixin config plugin probed Naturalist `Snail` with `Class.forName` during Mixin selection, which loaded the Entity hierarchy too early and led to Simple Copper Pipes' `EntityMixin` failure while Sodium preLaunch was active. This is retained external `RUNTIME_FAIL` provenance, not a Sodium or Simple Copper Pipes defect.

Canary 19 is `ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED`. Both canonical slots are currently occupied by unrelated cohorts, so do not change a slot merely to run this procedure. When a serialized Test Instance Manager slot is deliberately available, use only the dedicated Matcha Flavoured 26.2 Workbench; never use the protected Matcha Flavoured 26.1.2 gameplay instance. Rehash the retained private artifact before deployment and replace, never duplicate, the existing `ribbits` JAR.

Launch the ordinary managed stack with Ribbits C19, Naturalist, Simple Copper Pipes, Sodium, and the usual Matcha Flavoured 26.2 dependencies. Require normal startup past Fabric/Mixin preLaunch with no `MixinTargetAlreadyLoadedException`, `Entity was loaded too early`, Simple Copper Pipes `EntityMixin` preparation failure, Sodium preLaunch failure from this chain, or new Ribbits/Naturalist mixin application failure.

Verify a newly materialized merchant has exactly three native offers: 20 Glowcaps to Drop Leaf, 8 Glowcaps to the canonical explorer map, and one Compass to four Glowcaps. Upgrade an exact persisted C17 schema-3 six-offer merchant with used/exhausted offers and optional ranges: retain offer objects 0, 1, and 5; remove only native curiosity indexes 2–4; shift later ranges by three; and preserve uses, components, seeds, provider IDs, and policies. Malformed snapshots must remain unchanged.

With Naturalist installed, inspect scheduler-created snails only: each remains leashed to its Wandering Ribbit, with a lower snail origin and higher Ribbit endpoint; snails held by other entities remain unchanged. Verify the user-authored open Chute texture/model, deployed back pose, and first-person presentation. Record only observed behavior; do not call startup success visual verification unless that behavior was observed. Stop with FAIL on any menu, migration, leash ownership, visual, startup, or persistence regression.
