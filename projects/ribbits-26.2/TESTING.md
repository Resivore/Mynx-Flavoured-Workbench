# Testing

## Canary 18 — pending dedicated-slot runtime test

Canary 18 is `ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED`. Use only a deliberately available serialized Test Instance Manager slot in the dedicated Matcha Flavoured 26.2 Workbench. Never use the protected Matcha Flavoured 26.1.2 gameplay instance. Rehash the retained private artifact before deployment and replace, never duplicate, the existing `ribbits` JAR.

Verify a newly materialized merchant has exactly three native offers: 20 Glowcaps to Drop Leaf, 8 Glowcaps to the canonical explorer map, and one Compass to four Glowcaps. Upgrade an exact persisted C17 schema-3 six-offer merchant with used/exhausted offers and optional ranges: retain offer objects 0, 1, and 5; remove only native curiosity indexes 2–4; shift later ranges by three; and preserve uses, components, seeds, provider IDs, and policies. Malformed snapshots must remain unchanged.

With Naturalist installed, inspect scheduler-created snails only: each remains leashed to its Wandering Ribbit, with a lower snail origin and higher Ribbit endpoint; snails held by other entities remain unchanged. Verify the user-authored open Chute texture/model, deployed back pose, and first-person presentation. Record only observed behavior; stop with FAIL on any menu, migration, leash ownership, visual, startup, or persistence regression.
