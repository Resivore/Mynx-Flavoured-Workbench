# Testing

Mynx Ribbits Canary 4 is `ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED`. No Minecraft client or server was launched and no runtime state was changed while producing this candidate. All gameplay checks below remain pending and require a future serialized Test Instance Manager deployment into one canonical slot in the dedicated Minecraft 26.2 Workbench. Do not displace or combine another candidate, deploy into a personal profile, or touch the protected Matcha Flavoured 26.1.2 instance.

Before deployment, read the exact final filename, size, SHA-256, and source checkpoint from `WORKBENCH_STATUS.json`, then independently rehash the ignored `projects/ribbits-26.2/artifacts/ribbits-private-reconstruction-4.1.6+26.2-mynx-canary4.jar`. Its coordinated dependency must remain `YungsApi-26.2-Fabric-6.1.1-compat.2.jar`, 1,260,939 bytes, SHA-256 `FF22A6B509BA559988D7A9352DC94AC612C4B099517ACAC7DA7C81322D797ED7`. Never deploy the source-only archive.

Compilation, focused Java/Python tests, deterministic private assembly, source-only/private JAR production, archive diffing, and exact codec/registry/resource validation are static evidence only. They do not assign a Minecraft runtime result.

## Bound Phase B external report

The retained canonical Canary 3 private artifact was independently rehashed as 3,204,738 bytes with SHA-256 `69AD105B44E3E9131D540E26887EB09CE6BE1DA89EE9C4B3C49E4E95341F3E19`. The exact retained YUNG's API Compat.2 dependency was independently rehashed as 1,260,939 bytes with SHA-256 `FF22A6B509BA559988D7A9352DC94AC612C4B099517ACAC7DA7C81322D797ED7`. Those identities match authoritative `main`, so the user's report binds to that precise pair: Phase B is classified by the user as a practical gameplay `PASS`, and practical use looked sufficient to continue. The user intentionally did not execute the exhaustive profession-by-profession, tier-by-tier trade matrix. No individual unexecuted trade, component rejection, daily rotation, specialization, or restock edge case is thereby observed or passed. This external report does not promote Canary 3, assign a managed-slot result, or supply runtime evidence for Canary 4.

## Focused Canary 4 runtime matrix

1. Rehash the exact Canary 4 private artifact and YUNG's API Compat.2 before deployment; inspect embedded IDs/versions, compare against `WORKBENCH_STATUS.json`, and record deployment ID, slot, world seed, coordinates, client/server versions, and complete log paths.
2. Start a clean dedicated Minecraft 26.2 client/server pair and perform a data reload. Require no relevant startup, dependency, registry, recipe, item-component, structure-tag, loot-table, item-modifier, mixin, classloading, networking, or reload error.
3. Generate multiple new `minecraft:swamp_hut` structures in previously ungenerated chunks, covering all four orientations where practical. Also execute `/place structure minecraft:swamp_hut` and require the same new-hut behavior.
4. In every new hut require exactly one persistent Sorcerer, zero initial generated Witches, exactly one initial Cat, and exactly one map barrel. Require no overlap or blocked crafting table, cauldron, doorway, or window.
5. Verify the orientation-transformed local positions: Sorcerer `(3,2,5)`, Cat `(2,2,4)`, barrel `(2,2,6)`, crafting table `(3,2,6)`, and cauldron `(4,2,6)`. Require the barrel to be immediately left of the crafting table and its local state to originate as `minecraft:barrel[facing=north,open=false]`.
6. Verify the resident is exactly a `ribbits:sorcerer`, begins as Wart Whisperer with the complete Phase B Sorcerer economy plus failed-map redemption, has its home at the transformed spawn position, has normal umbrella/instrument data and the Sorcerer buff goal, remains in Peaceful, and persists across unload/reload and server restart.
7. Save/reload and restart without duplicate Sorcerers, Cats, barrels, or barrel loot. Re-enter or regenerate structure processing where safely possible and require the one-shot Witch and Cat flags and resolved barrel inventory to remain one-shot and independent.
8. Kill a hut-generated Sorcerer, unload/reload, and restart. Require no replacement Sorcerer and no initial Witch; record the deliberate one-shot resident behavior.
9. Attempt natural Witch spawning at every exact `SwampHutPiece` bounding-box face, representative corners, and empty upper layers. Require rejection throughout the inclusive piece box.
10. Attempt natural Witch spawning one block outside every face and at support-column positions below the piece. Require those candidates to remain permitted when all ordinary spawn rules pass.
11. Smoke-test preserved Witch paths inside a recognized hut: command or spawn egg must still create a Witch. Exercise a representative spawner/event path if practical, and require ordinary natural swamp Witches outside the exact piece to remain possible. Do not infer unexecuted paths.
12. Confirm a non-Witch monster natural-spawn candidate inside the piece is not rejected. Confirm invalid, absent, stripped, or malformed structure metadata fails closed as “not a recognized hut” and does not suppress broadly.
13. Confirm the initial Cat remains a black hut Cat at its new coordinate and that later `CatSpawner`, structure creature-spawn behavior, and ordinary Cats remain unchanged.
14. Open one barrel where a Ribbit village is found. Require exactly one `minecraft:filled_map` with a valid `minecraft:map_id`, normal position tracking, the plains-village marker, zoom 2, and the translated name `Ribbit Village Explorer Map`; verify it targets a selected `ribbits:ribbit_village` within the raw search radius 100 contract.
15. Resolve one controlled full radius-100 miss. Require exactly one vanilla `minecraft:map`, no map ID or village decoration, translated name `Uncharted Ribbit Map`, lore `No Ribbit village could be charted.`, and the private boolean custom-data marker `ribbits:failed_ribbit_village_map=true`. A normal miss must not throw or leave an empty barrel.
16. Save/reload each successful and failed barrel result, then generate/remove relevant structures or otherwise change the world. Require the stored result to remain fixed and require later inventory access not to reset the loot key or duplicate the item.
17. Trade one exact marked failed map with Sorcerers at Wart Whisperer, Gatecaller, Flask Sage, and Deep-Pond Oracle, plus representative command- and typed-egg-spawned Sorcerers. Require exactly one `ribbits:toadstool_heart`, maximum 16 uses per stock batch, zero profession XP, zero price multiplier, ordinary restocking only, and offer presence at every rank.
18. Require a generic empty map, an anvil-renamed empty map, a successful Ribbit Village Explorer Map, and an unrelated explorer map all to be rejected. Confirm matching uses the private marker, not name or lore.
19. Redeem at least one marked failed map before completing the Benzene gate and again afterward. Require no rank promotion, no gate completion or bypass, no changed Sorcerer specialization, and no alteration of ordinary Phase B Sorcerer offers.
20. Generate new Ribbit villages and inspect all affected future structures. Require zero Brewing Stands, Damaged Anvils, Smokers, and Blast Furnaces; require the two chimney replacements to be ordinary `minecraft:stone_bricks` and visually sound.
21. Confirm Barrels, Chests, all private loot bindings, Crafting Tables, Water Cauldrons, rooftop Campfires, beds, cakes, supporting counters/floors, and other retained structure features remain intact. Require no raw processor sentinel block to leak into final generated villages.
22. In an existing world, generate a hut in new chunks and require the full Phase C behavior. Inspect a previously generated hut and require no retrofit Sorcerer, barrel, or Cat relocation and no conversion/deletion of its saved Witch; require only exact natural-Witch suppression where valid piece metadata remains.
23. Run limited Phase B regression smoke checks only: representative Glowcap trades and strict component rejection, one profession through a rank advancement, Chef same-day menu persistence and day rotation, normal two-restocks-per-day persistence, Sorcerer Benzene gating, and Toadstool Heart home setting across reload. Do not require every trade permutation.
24. Measure or at least observe server-tick behavior for a nearby map hit and a full raw-radius-100 miss. Record visible stall duration and server warnings, if any; the synchronous server-authoritative search is intentional but unacceptable sustained stalls are a stopping condition.
25. Repeat the focused cases on a dedicated server with a remote client, including hut generation, resident/barrel sync, both map branches, failed-map redemption/rejections, new-village utility cleanup, and Phase B smoke behavior.
26. Review complete client and server logs. Preserve exact binaries, dependency identities, world/coordinates, logs, and row-level observations, and classify only the cases actually executed.

Stop and record the occupied slot independently as `FAIL` or `INCONCLUSIVE` if artifact identity drifts, the required pair is missing, manager readiness is not verified, any exact structure or item contract is approximated, mixin injection fails, resident/barrel generation duplicates or disappears, suppression escapes the exact procedural piece, a preserved spawn path is blocked, a normal map miss throws or yields no item, a generic map redeems, redemption changes rank/gate state, transformed utilities remain, retained storage/loot changes, a sentinel leaks, a relevant codec/world-generation/network error appears, or another required behavior fails. If focused rows pass but the matrix remains unfinished, keep lifecycle `TESTING`; do not promote or infer `PASS`.

No manual gameplay row above has been executed for Canary 4. An unrelated Phase B defect discovered later is a normal follow-up defect and does not retroactively manufacture an observation the user did not make.
