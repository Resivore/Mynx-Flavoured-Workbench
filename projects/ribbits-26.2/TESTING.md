# Testing

## Canary 20 — pending dedicated-slot runtime test

Supplied external C19 evidence records successful startup after the optional Naturalist mixin redesign, but two visual `RUNTIME_FAIL` findings: closed/undeployed Chute Leaf renders correctly while deployed/open Chute Leaf is black/magenta missing-resource checkerboard; the Naturalist snail leash renders but starts substantially above the snail and ends substantially below the Wandering Ribbit. C20 is `ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED`. Both canonical slots are occupied by unrelated cohorts, so do not change a slot merely to run this procedure.

When a serialized Test Instance Manager slot is deliberately available, use only the dedicated Matcha Flavoured 26.2 Workbench; never use the protected Matcha Flavoured 26.1.2 gameplay instance. Rehash retained `ribbits-private-reconstruction-4.1.6+26.2-mynx-canary20.jar` before deployment and replace, never duplicate, the existing `ribbits` JAR.

Launch the ordinary managed stack with Ribbits C20, Naturalist, Simple Copper Pipes, Sodium, and the usual Matcha Flavoured 26.2 dependencies. Require normal startup past Fabric/Mixin preLaunch with no `MixinTargetAlreadyLoadedException`, `Entity was loaded too early`, Simple Copper Pipes `EntityMixin` preparation failure, Sodium preLaunch failure from this chain, or new Ribbits/Naturalist mixin application failure. Repeat once without Naturalist when the managed stack permits it; the missing optional target must not impede startup.

Verify a newly materialized merchant has exactly three native offers: 20 Glowcaps to Drop Leaf, 8 Glowcaps to the canonical explorer map, and one Compass to four Glowcaps. Upgrade an exact persisted C17 schema-3 six-offer merchant with used/exhausted offers and optional ranges: retain offer objects 0, 1, and 5; remove only native curiosity indexes 2–4; shift later ranges by three; and preserve uses, components, seeds, provider IDs, and policies. Malformed snapshots must remain unchanged.

Inspect the closed Chute on the back, then the deployed authored canopy in third person and first person. With Naturalist installed, inspect scheduler-created snails only while stationary and moving, from multiple viewing angles: the snail endpoint must meet the body/shell area and the Wandering Ribbit endpoint must meet its upper body/hand side, without either substantial vertical float. Snails held by other entities remain unchanged. Record only observed behavior; do not call a runtime PASS from static evidence. Stop with FAIL on any menu, migration, leash ownership, visual, startup, or persistence regression.
