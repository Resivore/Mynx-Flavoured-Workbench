# Testing

Mynx Ribbits Canary 2 is `ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED`. All Minecraft gameplay checks below remain pending and require a separately authorized, serialized Test Instance Manager deployment to one canonical slot in the dedicated Minecraft 26.2 Workbench. A successful deployment must change lifecycle to `TESTING` and reach `READY_TO_TEST_VERIFIED` before testing begins.

Require the exact ignored private artifact `projects/ribbits-26.2/artifacts/ribbits-private-reconstruction-4.1.6+26.2-mynx-canary2.jar`, 3,166,148 bytes, SHA-256 `587B200A52300FF57C919ED5656E357DE5840D0D05C321D6D055405382D299BD`, from source checkpoint `f0be1f9c6c1f3e843a0e44791a650df237836537`. Its coordinated dependency is official project UUID `97d76c44-7c17-4b36-be84-57525f884e40`, version `26.2-Fabric-6.1.1-compat.2`, exact ignored artifact `projects/yungs-api-26.2/artifacts/YungsApi-26.2-Fabric-6.1.1-compat.2.jar`, 1,260,939 bytes, SHA-256 `FF22A6B509BA559988D7A9352DC94AC612C4B099517ACAC7DA7C81322D797ED7`, from the same source checkpoint. Deploy this pair atomically; never deploy the source-only archive, a substitute dependency, a personal profile, or the protected Matcha Flavoured 26.1.2 instance.

Conventional Minecraft datagen was unavailable for this candidate. Deterministic private resource assembly, complete tree/JAR validation, clean builds, focused fixtures, and static tests passed, but none is Minecraft runtime evidence.

## Unbound predecessor report

The canonical predecessor is Mynx Canary 1 `4.1.6+26.2-mynx-canary1`, filename `ribbits-private-reconstruction-4.1.6+26.2-mynx-canary1.jar`, 3,163,214 bytes, SHA-256 `EC2946D299BCDA22FFA393CE310760D1F963ED1D28377A86F45DA22E64869443`, source `3e9b81f2c2464811caf593a6451e6ff1c9136955`. Its canonical companion was `YungsApi-26.2-Fabric-6.1.1-compat.1.jar`, 1,266,121 bytes, SHA-256 `527850C4F061FA9AB327AE0B32A3D26EBC77B51FB86234F5F6B1E3418CA084CA`, from legacy source `57a89b593ce9410db98b75a350916ae36caf9522`.

The supplied failed-cohort files instead identified `ribbits-4.1.6+26.2-mynx-canary1.jar` at SHA-256 `e4470049225096bb37ff6da75a0db0beecc2caaa0e2c8c38c503f501feb390f` and `yungsapi-26.2-Fabric-6.1.1-compat.1.jar` at SHA-256 `bc2bdaf88417f445d656860cd2de133887f88ddb055fc9a922a68307a5c88043`, with typo source `57a89b5a9d1bf9fad90d39e368cb85a09460b621`. The user reported a `ribbits:ribbit_village` locate/teleport reproduction that reached `NoSuchElementException` in `EnhancedBeardifierHelper.computeDensity`, followed by `Parent chunk missing`, plus a creative-tab duplicate diagnostic in the same session. None of the supplied binary or source identities matches the canonical predecessor and no manager slot record binds them, so these observations remain unbound investigation input and are not a structured canonical Canary 1 runtime result.

## Manual Canary 2 runtime matrix

1. Rehash both artifacts, inspect their embedded mod IDs and versions, and verify the slot plan contains the exact Ribbits/YUNG pair before approving the atomic manager transition. Record the slot, deployment ID, world seed, coordinates, client/server versions, and log paths.
2. Start the dedicated client and server with Minecraft 26.2, Fabric Loader 0.19.3, Java 25, and a clean data reload. Require no relevant startup, registry, codec, resource, dependency, duplicate-item, or classloading error.
3. Create a fresh disposable world, locate `ribbits:ribbit_village`, teleport to it, and traverse every side far enough to generate its surrounding chunks. Repeat with a second freshly generated village, preferably in another disposable world.
4. During and after each village traversal, require no `EnhancedBeardifierHelper` error, `NoSuchElementException`, `Parent chunk missing`, world-generation crash, stalled chunk, or corrupted terrain. Preserve complete client and server logs.
5. Inspect the generated village/terrain boundary and require YUNG terrain adaptation to remain visibly functional; empty inputs must be harmless without globally disabling or flattening nonempty beardifier contribution.
6. Open the Ribbits creative tab and require `Mossy Oak Slab` exactly once, registered and obtainable, between the mossy oak stairs and fence. Require all 24 intended tab entries once, in their defined order, with no duplicate-item diagnostic.
7. Generate at least ten previously ungenerated Ribbit villages and sample at least 160 natural residents. Require every eligible profession—Musician, Gardener, Fisherman, Merchant, Chef, Farmer, Prospector, and Guard—to occur, no natural Sorcerer, and no persistent gross skew after the documented one-repeat allowance.
8. Exercise Chef, Farmer, Prospector, and Guard while idle, walking, sitting, sleeping, swimming, pathing, and exposed to rain. Verify their intended model, texture, accessories, all three umbrella variants, and no fallback, missing geometry, or donor behavior.
9. Save, exit, reload, change dimension, and restart with one entity of every new profession. Require stable profession IDs, umbrella variants, appearance, age, position, and ordinary Ribbit state.
10. Exercise all nine typed spawn eggs, pick-block mappings, and dispenser mappings. Each must create or return the exact profession; new professions must retain `RibbitInstrumentModule.NONE`, while the existing five retain established behavior.
11. Interact with adult Musician, Gardener, Sorcerer, Chef, Farmer, Prospector, and Guard Ribbits. Require no crash and no empty merchant screen.
12. Verify Merchant's established six offers and Fisherman's seven-definition pool with four randomly selected offers, including costs, ranges, `maxUses`, multipliers, serialization, and existing restock behavior. Do not expect a redesigned trade economy or strict daily limit.
13. Require both repaired private loot tables to load and function with no `Item must not be minecraft:air`, codec, or registry error.
14. Inspect resources, structures, behavior, and logs for donor leakage. Require no donor AI, classes, attacks, procedures, structures, world generation, workstations, inventories, GUIs, sounds, or non-allowlisted content.
15. Run the focused matrix on a dedicated server with at least one remote client, then save, disconnect, restart, and repeat village generation, one rain/umbrella case per new profession, eggs, interaction, trades, persistence, and final log review. Require no client/common split, payload, synchronization, or server crash.

Stop and record the occupied slot independently as `FAIL` or `INCONCLUSIVE` if artifact identity drifts, either artifact is missing, the manager does not reach verified readiness, village traversal reproduces a relevant exception, the creative-tab contract fails, terrain adaptation disappears, any profession/resource/loot/trade/persistence/network contract fails, or a relevant crash or log error occurs. If the focused defect checks pass but the broader manual matrix is incomplete, keep lifecycle `TESTING`; do not promote or infer `PASS`.

No manual gameplay row has been executed for Canary 2. Compilation, fixtures, deterministic assembly, archive inspection, validator success, or an unexecuted procedure cannot assign a runtime result.
