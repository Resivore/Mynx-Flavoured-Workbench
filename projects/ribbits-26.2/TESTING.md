# Testing

Mynx Ribbits Canary 1 is `ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED`. This matrix is for a future explicitly authorized dedicated Workbench task only. Before deployment, require the exact ignored private artifact `projects/ribbits-26.2/artifacts/ribbits-private-reconstruction-4.1.6+26.2-mynx-canary1.jar`, 3,163,214 bytes, SHA-256 `EC2946D299BCDA22FFA393CE310760D1F963ED1D28377A86F45DA22E64869443`, and exact ignored dependency `projects/ribbits-26.2/artifacts/YungsApi-26.2-Fabric-6.1.1-compat.1.jar`, 1,266,121 bytes, SHA-256 `527850C4F061FA9AB327AE0B32A3D26EBC77B51FB86234F5F6B1E3418CA084CA`.

Never deploy `ribbits-source-only-4.1.6+26.2-mynx-canary1.jar`; it is resource-incomplete and non-runnable. Do not use historical Canary 2, a personal profile, or the protected Matcha Flavoured 26.1.2 instance for this matrix. A future runtime task must acquire one canonical Test Instance Manager slot before touching the dedicated Minecraft 26.2 Workbench.

## Manual Canary 1 runtime matrix

1. Start the dedicated client and server with a clean data reload, create a disposable world, and inspect complete logs. Require no decode, registry, or `Item must not be minecraft:air` error for `ribbits:chests/fisherman_main` or `ribbits:chests/sorcerer`.
2. Generate at least ten previously ungenerated Ribbit villages across fresh chunks. Record world seed, village coordinates, and resident counts; do not count typed-egg or command-spawned Ribbits.
3. Across those natural villages, observe Chef, Farmer, Prospector, and Guard residents without commands or eggs.
4. Across the same natural sample, observe Musician, Gardener, Fisherman, and Merchant residents.
5. Require zero naturally assigned Sorcerers in every generated Ribbit village. Separately prove Sorcerer still works through its typed spawn egg and a direct command.
6. Sample at least 160 natural village residents from at least ten villages. Record counts for all eight eligible professions; require all eight to occur and no profession count to exceed 2.5 times another. If the threshold is missed, repeat once with a separate fresh 160-resident sample and classify persistent gross skew as failed and a one-sample anomaly as inconclusive.
7. Inspect Chef, Farmer, Prospector, and Guard while idle, walking, sitting, sleeping, swimming, and pathing. Require their intended model, texture, accessories, animations, UVs, and visibility with no fallback to another profession.
8. Expose each new profession to rain and umbrella-falling behavior. Require its profession appearance and accessories to remain visible with a complete umbrella and valid texture.
9. For each of Chef, Farmer, Prospector, and Guard, observe umbrella variants 1, 2, and 3, using controlled entity data only if random spawning is insufficient. Require all twelve profession/variant combinations to render correctly.
10. Inspect Guard's modeled spear and shield. Require both to remain cosmetic: no attack, targeting, defense, alert, patrol, structure-injection, or other donor combat behavior may occur.
11. Save, exit, reload, change dimension, and restart once with one entity of every new profession. Require profession ID, umbrella variant, appearance, age, position, and ordinary Ribbit state to persist without fallback or corruption.
12. Use all nine typed Ribbit spawn eggs. Require each egg to create its exact profession; the four new professions must initialize with `RibbitInstrumentModule.NONE`, and the existing five eggs must retain their established behavior.
13. Use pick-block on all nine professions and require the corresponding typed egg ID, including safe nitwit/Musician fallback only for a deliberately unknown profession test case.
14. Dispense each of the nine eggs from a dispenser and require the exact typed profession. For the four new eggs, also require no instrument assignment.
15. Exercise generic idling near instruments and normal movement for Chef, Farmer, Prospector, and Guard. Require no instrument playback, instrument-model swap, or donor-specific behavior; preserve Musician, Gardener, Fisherman, Sorcerer, and Merchant behavior.
16. Interact with adult Musician, Gardener, Sorcerer, Chef, Farmer, Prospector, and Guard Ribbits. Require no crash and no empty merchant screen.
17. Inspect Merchant offers and require all six established definitions: brown toadstool, red toadstool, toadstool stem, mossy oak planks, swamp lantern, and maraca, with unchanged costs, output ranges, `maxUses`, and price multipliers. Exhaust and restock offers only to confirm the existing implementation; do not expect a new strict daily limit.
18. Inspect repeated Fisherman spawns and require the established seven-definition pool—axolotl bucket, tropical fish bucket, cooked cod, cooked salmon, enchanted fishing rod, cod purchase, and salmon purchase—with exactly four randomly selected offers per Fisherman and unchanged ranges, `maxUses`, multipliers, serialization, and restock behavior.
19. Inspect server behavior, loaded structures, resources, and logs for donor leakage. Require no donor AI, Java/classes, procedures, attacks, structures, world generation, workstations, inventories, GUIs, sounds, spawn-egg graphics, mines, hoists, beds, or chests.
20. Save cleanly, disconnect every client, stop the server, restart both sides, rejoin, and repeat core profession, loot, trade, rendering, and persistence checks. Review final client and server logs before assigning a result.
21. On the dedicated server host, run a fresh multiplayer smoke world with at least one remote client. Repeat data reload, village generation, each new profession, one rain/umbrella case per new profession, typed eggs, pick/dispenser behavior, trade-less interaction, Merchant/Fisherman offers, save/restart, and log review; require no common/client split, payload, synchronization, or dedicated-server crash.

Stop and record the independent slot result as `FAIL` or `INCONCLUSIVE` if the candidate or dependency identity differs, a private resource is missing, either loot table fails to load, natural villages contain Sorcerers, any eligible profession cannot be observed after the defined repeat sample, a new profession falls back or renders incorrectly, an egg/pick/dispenser mapping is wrong, an instrument or donor behavior appears on a new profession, a trade-less screen opens, Merchant/Fisherman semantics differ, save/reload fails, donor leakage appears, client/server behavior desynchronizes, logs contain a relevant codec/registry/model/resource/payload error, or the game crashes.

Do not promote, deploy elsewhere, assign a runtime pass, or describe Canary 1 as runtime-correct from compilation, unit tests, exact codec decoding, static tree/JAR validation, deterministic artifacts, or this unexecuted procedure.
