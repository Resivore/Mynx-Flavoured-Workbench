# Testing

Ribbits private Canary 1 is `NOT_DEPLOYED / RUNTIME_UNTESTED`. Use this procedure only during a future explicitly authorized runtime task with both the exact private, Git-ignored local artifact `projects/ribbits-26.2/artifacts/ribbits-4.1.6+26.2-port-canary1.jar`, 3,135,359 bytes, SHA-256 `886F2586CA8D5AB80BDFC58CD18E10DC6BA4FF180371AB71461681DB5BA00B6D`, and the exact Git-ignored dependency `projects/ribbits-26.2/artifacts/YungsApi-26.2-Fabric-6.1.1-compat.1.jar`, 1,266,121 bytes, SHA-256 `527850C4F061FA9AB327AE0B32A3D26EBC77B51FB86234F5F6B1E3418CA084CA`. The public `ribbits-source-only-4.1.6+26.2-port-canary1.jar` is resource-incomplete and disposable; never deploy or classify it as Canary 1.

## Focused runtime matrix

1. Confirm the exact private artifact and required dependency identities, then launch and review the complete log for registry, codec, mixin, model, resource, or payload errors.
2. Create a disposable test world, save, restart, change dimensions, and reload the world; confirm Ribbit entities and their profession, instrument, umbrella, pride, and supporter state persist.
3. Exercise all five professions and their expected goals, trades, work, home, crop-watering, fishing, music, buff, and interaction behavior without crashes or incorrect profession fallback.
4. Verify models, profession skins, umbrellas, animations, sounds, particles, maraca playback, supporter hats, and client disconnect/reload cleanup.
5. Verify all five distinctly named profession spawn eggs share the authorized temporary green texture while preserving the correct spawned profession, dispenser behavior, persistence, and pick-result identity.
6. Locate or generate the Ribbits village and exercise its processors, vegetation, blocks, items, tags, recipes, advancements, and sorcerer loot behavior.
7. On the actual server host during an explicitly scheduled server playtest, start a disposable world, join with a client, and verify common/client separation plus all music, maraca, and supporter payload directions without disconnects or desynchronization.
8. Save and restart once more, repeat the core entity and world-generation checks, and review the final client and server logs before assigning any runtime classification.

Stop and record the result as failed or inconclusive if exact artifact or dependency identity differs, required private resources are missing, any registry/codec/mixin/model/payload error appears, state fails to persist, client/server behavior desynchronizes, or the game crashes. Do not promote or call the project validated from source/static evidence alone.
