# Testing

Candidate `villager-work-routines-0.1.0-canary22.jar` / SHA-256 `ea84e98021ec599699a22676697e1cd837ada3953c74e6546598af16f7625a51` / source `b874aad7388d7f29e325f9886549c69269c3f949` is a user-managed Minecraft Java 26.2/Fabric diagnostic trial. Verify that identity against `WORKBENCH_STATUS.json`, together with exact retained Ribbits Canary 27 `ribbits-private-reconstruction-4.1.6+26.2-mynx-canary27.jar` / SHA-256 `088c4b7e88c432d6275395e29273597cf42575f59350fe8b4f48c46e6df7f9fd`. Use the normal Frog Villager resource pack. Build and static validation are not runtime evidence. Do not install the Real Working Villagers reference JAR or access a protected Minecraft profile.

## Canary 22 Fisherman rod transform diagnostic trial

### Historical evidence boundary

Exact C20 runtime testing is **FAIL** only for C20: the Ribbits rod rendered around/above the Frog Villager's head and upper torso instead of at the visible folded hands; its angle/placement did not match the supplied Blockbench reference; and VWR's line was detached from the visible outer shaft tip toward an incorrect distant/high world-space point. The owner's exact C21 observation is limited to: **the rod was not visibly present**. Do not infer its cause, a C21 line result, any broader behavior result, or a C22 result. C22 deliberately restores C20's visual rod and line paths to collect evidence; it is not a pose correction.

Run this short trial at normal **20 TPS**. Accelerated ticks may reach vanilla WORK time only; restore 20 TPS before casting, rendering, or observation.

1. Start Minecraft with exact VWR C22 and Ribbits C27, with ordinary INFO logging retained. Reach normal client initialization with the normal Frog Villager resources enabled; there must be no VWR mixin, GeckoLib, entity-renderer, or resource-resolution failure.
2. Give one ordinary VWR Fisherman its claimed barrel, valid bounded open source water, and a safe adjacent bank. Wait for one live cast and capture one screenshot showing the villager, restored C20 rod/line, and diagnostic markers. The legend is: white cross = incoming entity-layer origin; cyan square = post-`translateToArms` origin; magenta X = authored `[0,-7,-6]` grip; orange triangle = GeckoLib geometry root; yellow diamond = live `fishing_rod` pivot; green star = physical outer shaft tip; red/green/blue rays = each marker's local X/Y/Z axes.
3. Retain the concise `[VWR C22 ROD DIAGNOSTIC: ...]` INFO blocks. They must identify the exact C27 model/texture and hashes, actual root-to-`fishing_rod` chain values and `fishing_rod_2`/`fishing_rod_3`, runtime `VillagerModel` class, actual `arms` state/direct children/recursive descendants and explicit `arms_rotation` result, named-space incoming → arms → grip → Geo matrices, live physical pivot/tip, the current C20 line-start coordinate, and the diagnostic tip delta.
4. Confirm repeated frames do not spam equivalent resource/model, hierarchy, or cast blocks. Confirm the markers disappear when the live cast ends. The rod and line should retain C20 behavior—including any displacement already known from C20—and the line must still use C20's analytical point rather than silently moving to the newly measured physical tip.
5. End after this one-cast evidence capture. If a catch/deposit or Shepherd path happens to be observed, record only what was actually seen; this focused trial does not require a broader behavior pass.

Record **FAIL** for any startup/client/resource error; missing required checkpoint, marker, or log fact; equivalent per-frame diagnostic spam; diagnostic instrumentation suppressing the restored C20 rod or line; a line silently rerouted to the measured tip; or an actually observed Fisherman nonvisual or Shepherd regression. Return the exact screenshot and concise log blocks without inferring a cause beyond the recorded evidence. C22 remains **ACTIVE / STATIC_PASS / RUNTIME_UNTESTED** until this owner-managed trial is performed.
