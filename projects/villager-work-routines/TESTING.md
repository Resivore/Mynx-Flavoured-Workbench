# Testing

Candidate `villager-work-routines-0.1.0-canary23.jar` / SHA-256 `c794827a92f33be7da486608f782d16d66c99f0dfb0c7da1d2f6b49e4e14c721` / source `dc10f14a98e4c1658e6175de68426947ac298df8` is a user-managed Minecraft Java 26.2/Fabric correction trial. Verify that identity against `WORKBENCH_STATUS.json`, together with exact retained Ribbits Canary 27 `ribbits-private-reconstruction-4.1.6+26.2-mynx-canary27.jar` / SHA-256 `088c4b7e88c432d6275395e29273597cf42575f59350fe8b4f48c46e6df7f9fd`. Use the normal Frog Villager resource pack. Build and static validation are not runtime evidence. Do not install the Real Working Villagers reference JAR or access a protected Minecraft profile.

## Canary 23 Fisherman folded-hand rod/line correction trial

### Historical evidence boundary

The supplied result is **C22 runtime FAIL evidence only**: C22 rendered the C27 rod, but the entire rod/grip was displaced across/above the Frog Villager rather than attached to the visible folded hands. Its authored grip and live `fishing_rod` pivot had the same rendered origin, establishing only that the standalone rebase was internally coherent. The observed live hierarchy was `arms -> EMF_arms -> EMF_arms_rotation -> EMF_arms_flipped`; `arms_rotation_direct_child_exists=false`. The C22 analytical line start minus the measured physical outer tip was `(-0.133268, +0.541128, +0.101276)`, distance `0.566425` blocks. Do not infer any other C22 behavior result, and do not treat this as C23 evidence. C23 is **ACTIVE / STATIC_PASS / RUNTIME_UNTESTED**.

Run this focused trial at normal **20 TPS**. Accelerated ticks may reach vanilla WORK time only; restore 20 TPS before casting, rendering, or observation.

1. Start Minecraft with exact VWR C23 and Ribbits C27, with ordinary INFO logging retained. Reach normal client initialization with the normal Frog Villager resources enabled; there must be no VWR mixin, GeckoLib, entity-renderer, model/resource-resolution, or folded-arm-path failure.
2. Give one ordinary VWR Fisherman its claimed barrel, valid bounded open-source water, and a safe adjacent bank. Wait for one live cast and capture one screenshot that clearly shows the villager, rod, line, float, and diagnostic markers.
3. Verify that the rod grip visibly sits at the Frog Villager's folded hands and that the shaft/reel/detail match the supplied Blockbench reference pose rather than crossing the head or torso. There must be no visible `fishing_rod_2` / `fishing_rod_3` duplicate decoration, vanilla fishing equipment, second line, or second bobber.
4. Verify the marker chain: white cross = incoming entity-layer origin; cyan square = outer post-`translateToArms` origin; blue plus = effective visible folded-arm attachment; magenta X = exact authored `[0,-7,-6]` grip; orange triangle = GeckoLib geometry root; yellow diamond = live `fishing_rod` pivot; green star = physical outer shaft tip; red/green/blue rays = local X/Y/Z axes. The magenta grip must be at the intended folded hands, and the green star must be at the visible outer shaft end.
5. Verify that VWR's sole 16-segment line begins at that same green-star physical shaft tip, with no detached or arbitrary high origin, and ends at the existing float. Retain the concise `[VWR C23 ROD DIAGNOSTIC: ...]` INFO blocks. They must identify exact C27 model/texture bytes, the live Geo chain, the structural folded-arm selection rule and actual effective path, named transform checkpoints, the physical tip, and a line-start-minus-visible-tip distance of zero within displayed precision. The reported path may contain runtime wrapper names as observations but must not claim a nonexistent direct `arms_rotation` child.
6. Confirm equivalent resource/model, hierarchy, and cast diagnostics do not spam every frame and all markers disappear when the cast ends. Confirm the normal cast/retrieve cycle, one float lifecycle, successful fish generation/deposit and sounds if naturally reached, cleanup, and one available Shepherd visual/feedback path show no actually observed regression. Record only behavior that is actually observed.

Record **FAIL** for any startup/client/resource/path error; rod absent or displaced from the visible folded hands; pose inconsistent with the supplied reference; duplicate rod/line/bobber geometry; authored grip marker away from the hands; physical-tip marker away from the visible shaft end; line start detached from that physical tip; missing required diagnostic fact; equivalent per-frame diagnostic spam; or an actually observed Fisherman nonvisual or Shepherd regression. Return the exact screenshot and concise diagnostic blocks. C23 remains **ACTIVE / STATIC_PASS / RUNTIME_UNTESTED** until this owner-managed trial is performed.
