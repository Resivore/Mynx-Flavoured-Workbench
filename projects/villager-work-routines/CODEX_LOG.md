# Codex Log

## 2026-09-13T14:44:24Z — Implement Villager Work Routines Canary 1

- Revision: 1
- Source checkpoint: `123214fb32f5374cab3ee61637bb3ab5dfbbcacc`
- Changes: Added a new independent 26.2 Fabric mod. A narrow villager tick hook requires vanilla WORK activity and the exact claimed job-site memory/POI. Shepherds navigate to adult shearable sheep and call normal sheep shearing; only the callback output of that exact synchronous shear enters a separate persisted owned-output container, never nearby ItemEntity output. Wool deposits use component-identical matching stacks before empty slots across deterministic face-adjacent loom barrels. Fishermen choose a reachable nearby bank, show a temporary rod and isolated visible float/line, sample the vanilla fish-only loot table with an explicit four-fish allowlist, retain their output, and insert only into their claimed barrel. Ten other professions get claimed-workstation look/swing/work sound only. Farmer, Nitwit, Matcha definitions/trades, villager models/renderers/textures, global brain/social memories, and workstation inventories are untouched. Source is independent; the reference JAR is not bundled.
- Build/static: Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed; four focused storage tests passed (matching/empty insertion, full container preservation, component/color separation, multiple-barrel precedence). `git diff --cached --check` passed. The mapped 26.2 Sheep bytecode target for the exact shear-output callback was inspected. Packaged mod metadata and isolated 15-class JAR were inspected; no reference classes are present. No game/client or Workbench runtime behavior was observed.
- Runtime: NOT_DEPLOYED / RUNTIME_UNTESTED. Canonical slots A and B were occupied by other projects at inspection; no Test Instance Manager operation or Minecraft profile was touched. The frog-villager resource-pack fit and Matcha in-game compatibility remain unverified.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary1.jar`, 31,295 bytes, SHA-256 `af0b3ba222a8b5e9fc51ef4373ef27f4210d0dfd2ced62e32c550cc1e2eb4526`, source `123214fb32f5374cab3ee61637bb3ab5dfbbcacc`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. Local behavioral reference `originals/mods/Real Working Villagers.jar` was read-only and SHA-256 verified as `95d404c36605c5e08350bf632731b6764d64db444e43357a1b434abd099b9610`; embedded identity is `Villager Brain` 1.0.0, `~26.2`, CC0-1.0. Public Real Working Villagers release metadata is distinct from that embedded JAR identity and was not substituted for it.
- Result: ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED; no acceptance claim.
- Next state: Retain exact ignored artifact bytes in the primary local checkout, integrate/push the validated project revision to `main` for Sheet reconciliation, then obtain a serialized free Test Slot and complete `TESTING.md` in the dedicated 26.2 Workbench with the frog resource pack before any acceptance decision.

## 2026-09-14T08:28:48Z — Implement Villager Work Routines Canary 2 after Canary 1 runtime FAIL

- Revision: 2
- Source checkpoint: `65e92c427a2d102e854e4c50f0aa151ced541b5b`
- Changes: Recorded the exact user-reported external Canary 1 FAIL for `0.1.0-canary1`, `villager-work-routines-0.1.0-canary1.jar`, SHA-256 `af0b3ba222a8b5e9fc51ef4373ef27f4210d0dfd2ced62e32c550cc1e2eb4526`, source `123214fb32f5374cab3ee61637bb3ab5dfbbcacc`. Shepherd selection now requires a reachable walkable interaction block, body-height collision visibility and actual arrival; it faces the sheep and holds temporary shears for 14 ticks before the shear/swing and five ticks after, with revalidation and cancellation. Fisherman shoreline search accepts a standing block above ordinary solid shore next to open source water, checks loaded dry body space/support/collision and a reachable exact path, then navigates, equips the rod, casts, retrieves, retains fish-only output, and returns to the claimed barrel. Rate-limited `[VillagerWorkRoutines]` stage/scan/cancellation diagnostics identify target, path, action, capture and deposit outcomes. Ownership/storage, ten cosmetic professions, vanilla Farmer/Nitwit/unemployed behavior, and isolated float renderer remain unchanged.
- Build/static: Clean Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed; eight focused tests passed (four existing storage cases and four shoreline/standing-position/deterministic-selection cases). `git diff --cached --check` passed for the implementation checkpoint. Packaged Canary 2 metadata and isolated JAR entries were inspected; release runtime dependency policy is `CAPABILITY_OR_PROVIDER` with no exceptions. No Minecraft runtime behavior was observed for Canary 2.
- Runtime: User-reported external Canary 1 **FAIL**, with no managed deployment claimed here. A shepherd sheared one sheep and deposited exactly three wool in a loom-adjacent barrel, but did not visibly enter/approach the pen, show shears, or show a clear shearing action; the sheep nevertheless became sheared from an implausible position. In a real village with eligible water, a fisherman repeatedly worked at its claimed barrel but never visibly traveled to water; no rod, cast, float/line, splash, catch, or fish deposit was observed and the barrel stayed empty. This is a custom fishing routine failure, not insufficient test time. Canary 2 is NOT_DEPLOYED / RUNTIME_UNTESTED; its next game trial is user-managed.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary2.jar`, 41,521 bytes, SHA-256 `f6435fdc293b17ba03c1c98d0fac7f82b6bcc9ae7280a87ea927a7f8ac9313ce`, source `65e92c427a2d102e854e4c50f0aa151ced541b5b`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The ignored current artifact requires canonical local retention; no private/reference contents were packaged.
- Result: Canary 1 external runtime FAIL preserved; current Canary 2 ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, with no runtime acceptance claim.
- Next state: Retain the exact ignored Canary 2 artifact locally, integrate/push revision 2 to `main` for the project Sheet workflow, and have the user run the focused `TESTING.md` game trial with the frog-villager pack and diagnostics before any acceptance decision.

## 2026-09-14T09:38:53Z — Implement Villager Work Routines Canary 3 after Canary 2 Shepherd runtime FAIL

- Revision: 3
- Source checkpoint: `29eda30684a28957b47b29f7ac8b89e980d9bdd2`
- Changes: Recorded the exact user-reported external Canary 2 Shepherd FAIL for `0.1.0-canary2`, `villager-work-routines-0.1.0-canary2.jar`, SHA-256 `f6435fdc293b17ba03c1c98d0fac7f82b6bcc9ae7280a87ea927a7f8ac9313ce`, source `65e92c427a2d102e854e4c50f0aa151ced541b5b`. Canary 3 keeps legitimate collision-clear, supported, dry interaction positions and an unobstructed shear line, but judges a vanilla path by its actual final node instead of requiring `canReach()` and an exact `Path#getTarget()` match. A nearby partial endpoint is accepted only if it independently passes standing, sheep-distance, and endpoint shear-line checks; `moveTo` must accept it, and the Shepherd must physically arrive with its own safe footing, sheep distance at most 2.4 blocks, and a clear line before the existing 14-tick visible shears telegraph/swing/shear. An already physically valid Shepherd position can begin the action without starting a redundant path. Rate-limited per-sheep route summaries now include requested position, path null/reach status, reported target, endpoint, deltas, `moveTo`, and rejection reason; selected route, arrival, action, capture, and wool deposit stages remain logged. Eligibility transitions now distinguish dead/baby/sleeping/trading/breeding/WORK/site/dimension/block/POI/profession conditions. Vanilla 26.2 WORK may set `INTERACTION_TARGET` simply to look at a nearby player, so that memory alone no longer cancels Shepherd/Fisherman work; ambient profession gating and behavior remain unchanged. Closed fence gates remain nontraversable to vanilla navigation and are not opened by this Canary; already-open gates are ordinary routes. Fisherman shoreline/navigation and diagnostics, owned-output rules, and the ten cosmetic professions were otherwise unchanged.
- Build/static: Clean Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed; 11 focused tests passed (four storage, four unchanged shoreline, three new path-endpoint tolerance cases). `git diff --cached --check` passed for the implementation checkpoint. Inspected packaged `fabric.mod.json` capability dependency policy and isolated JAR entries. Local mapped Minecraft 26.2 navigation bytecode confirmed `GroundPathNavigation` may normalize the requested Y, `Path#getEndNode()` is the actual endpoint, and `PathNavigation#moveTo` may accept a partial path without `canReach()`; vanilla WORK's look/interact task explains the nearby-player memory flicker. No Minecraft runtime behavior was observed for Canary 3.
- Runtime: User-reported external Canary 2 Shepherd **FAIL** in a creative/superflat setup with a claimed loom and three eligible adult sheep. The repeated scan reported `found=3 examined=3 baby=0 notShearable=0 noCapacity=0 noValidInteraction=0 noPath=3 selected=none`; none was selected, no custom navigation began, and no shears/action/shear occurred. The log repeatedly alternated `custom WORK eligible` and `custom WORK cancelled: WORK eligibility interrupted` within short periods. The Canary 2 trial supplied no independent Fisherman runtime evidence. Canary 3 is NOT_DEPLOYED / RUNTIME_UNTESTED; runtime deployment/testing is user-managed.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary3.jar`, 47,528 bytes, SHA-256 `7ee0fc48b9e871bff2b7258b42071a9e5d5a4bf4c1728ed04fd857a0beeab9bd`, source `29eda30684a28957b47b29f7ac8b89e980d9bdd2`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. No reference contents were packaged. `tools/artifact_retention.py` copied and byte/hash-verified the exact ignored JAR in the primary local checkout without tracking it.
- Result: Canary 2 external Shepherd runtime FAIL preserved; current Canary 3 ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, with no runtime acceptance claim and no Fisherman PASS claim.
- Next state: Integrate/push revision 3 to `main` for the project Sheet workflow, then have the user run the focused `TESTING.md` Shepherd trial before any acceptance decision.

## 2026-09-14T15:25:44Z — Implement Villager Work Routines Canary 4 after Canary 3 Shepherd runtime FAIL

- Revision: 4
- Source checkpoint: `9e0edfe95b1959b2256a1255767509a50d8e60d2`
- Changes: Recorded the exact user-reported external Canary 3 Shepherd FAIL for `0.1.0-canary3`, `villager-work-routines-0.1.0-canary3.jar`, SHA-256 `7ee0fc48b9e871bff2b7258b42071a9e5d5a4bf4c1728ed04fd857a0beeab9bd`, source `29eda30684a28957b47b29f7ac8b89e980d9bdd2`. Canary 4 preserves C3's direct-path endpoint, safe-footing, distance, and clear shear-line rules; only after a direct route fails for a known eligible sheep does it inspect bounded local fence gates. A candidate gate needs a reachable near side and a collision-clear far-side route to an interaction position for that same sheep; deterministic path cost and coordinates choose among valid gates. The Shepherd alone approaches and opens the gate, obtains a fresh vanilla path through it, verifies physical crossing, safely closes a routine-opened unpowered gate only after clearing its unoccupied passage, continues to the retained sheep, keeps the 14-tick visible shears phase and exact owned shear output, then returns through the known gate before loom deposit. Originally-open, externally changed, powered, occupied, missing, or blocked gates are handled without global villager navigation or gate permissions. Interruption and save/load retain bounded exit or safe restoration obligations; death/conversion makes only safe restoration attempts. Fisherman, owned-output/storage, and ambient professions remain otherwise unchanged.
- Build/static: Clean Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed; 19 focused tests across four suites passed, including eight new target-route/ownership/stage tests. `git diff --cached --check` passed for the implementation checkpoint. Packaged `fabric.mod.json` retains capability-based dependency policy `CAPABILITY_OR_PROVIDER` without exceptions; the 65,572-byte Canary 4 JAR contains only this project's classes and metadata, with no reference JAR or unrelated project contents. Mapped 26.2 `FenceGateBlock` facing, open/powered/collision/path semantics and vanilla gate sound/game-event APIs were inspected. No Minecraft runtime behavior was observed for Canary 4.
- Runtime: User-reported external Canary 3 Shepherd **FAIL** in a controlled sheep pen with a correct claimed loom, three adult shearable sheep, output capacity, and valid interaction points. All vanilla paths stopped at the enclosure boundary, 2–3 blocks short of the intended interaction point, with `canReach=false`, `reason=endpointOutsideTolerance`, and `barrierToSheep=true`; repeated scans ended `noPath=3 selected=none`. Canary 3 did not open/traverse the closed fence gate; no sheep was selected, shears phase reached, or shearing performed. No Fisherman verdict was supplied. Canary 4 is NOT_DEPLOYED / RUNTIME_UNTESTED; runtime deployment and testing are user-managed.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary4.jar`, 65,572 bytes, SHA-256 `c7ff8c6194f389e71ce3cfb52a15d62d261156bdf9f00501e092294be7dc7c70`, source `9e0edfe95b1959b2256a1255767509a50d8e60d2`, release runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The current ignored JAR requires exact canonical local retention.
- Result: Canary 3 external Shepherd runtime FAIL preserved; current Canary 4 ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, without a Shepherd or Fisherman runtime PASS claim.
- Next state: Verify exact local artifact retention, integrate/push revision 4 to authoritative `main` for the main-only Sheet reconciliation workflow, then have the user run the 20-TPS `TESTING.md` gate-entry/exit scenarios before any acceptance decision.

## 2026-09-15T02:13:19Z — Implement Villager Work Routines Canary 5 after Canary 4 Shepherd runtime FAIL

- Revision: 5
- Source checkpoint: `0b893ba16d938fa8abc7e43723f6f947630d2e1c`
- Changes: Recorded the exact user-reported external Canary 4 Shepherd FAIL / partial functional success for `0.1.0-canary4`, `villager-work-routines-0.1.0-canary4.jar`, SHA-256 `c7ff8c6194f389e71ce3cfb52a15d62d261156bdf9f00501e092294be7dc7c70`, source `9e0edfe95b1959b2256a1255767509a50d8e60d2`. C4 successfully discovered eligible sheep from outside, fell back from a blocked direct path to the correct target-driven gate, approached/opened/crossed/closed it, retained the selected sheep, continued inward, and was observed returning through and closing the known gate; Canary 1 through-fence shearing did not recur. C4 repeatedly cancelled on normal main-hand occupancy, synthetic shears replacement, small target movement/interaction invalidation, and `endpointTooFarFromSheep`, never produced a reliable completed shear cycle, and allowed sheep to escape through the Shepherd-opened gate. Canary 5 preserves the working route system. It replaces reference-identity props with a zero-drop, semantically marked transaction that copies and restores exact legitimate stack/drop state, rebases genuine external changes, quarantines stale VWR props, and clears across success, cancellation, save/load, death, conversion, profession change, and removal. The Shepherd now approaches to about 1.8 blocks, tracks the same sheep through a 14-tick telegraph, repositions without gate rescanning, and requires final safe footing, clear line, shearability, output capacity, and at most 2.4-block distance before a verified real shear. A transient world-aware registry gives villager/task owners independent leases on VWR-opened gates; the exact 26.2 walk-node hook classifies those gates as vanilla `FENCE` only for Sheep, Cow, Pig, Chicken, and Rabbit. Registration covers the synchronous open interval, entry/exit/cancellation/resumed obligations, multiple owners, external invalidation, and world cleanup. Closure uses the hypothetical closed gate's actual collision shape, retains protection while genuinely occupied or while a close mutation fails, and lets only the last owner close. Fisherman, owned wool/storage, ambient professions, and ordinary/player-opened gates remain otherwise unchanged; no butcher or rancher behavior was added.
- Build/static: Clean Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed; 44 focused tests across eight suites passed with zero failures, errors, or skips. Coverage includes semantic/copy-safe prop ownership, empty and occupied originals, exact stack/drop restoration, external replacement/clear, stale-prop quarantine, prop-type switching, same-target shear decisions and final-validation gating, five-species path scope, unrelated entity exclusions, world/position/owner isolation, identity invalidation, transient cleanup, shared-owner release, safe last-owner closure planning, route stages, output storage, and Fisherman shoreline helpers. `git diff --check` and `python -B tools/workbench.py validate-repository --root .` passed; repository validation covered 59 manifests. The packaged JAR and `fabric.mod.json` were inspected: Canary 5, exact Minecraft 26.2, minimum Fabric Loader/API/Java floors, `CAPABILITY_OR_PROVIDER` without exceptions, required `WalkNodeEvaluatorMixin`, production classes/resources only, and no test/reference/unrelated project content. No Minecraft runtime behavior was observed for Canary 5.
- Runtime: User-reported external Canary 4 Shepherd **FAIL** / partial functional success with the working and failing observations above. No independent Fisherman verdict was supplied. Canary 5 is NOT_DEPLOYED / RUNTIME_UNTESTED; no Test Instance Manager state or Minecraft profile was inspected, managed, or modified, and its next runtime trial is user-managed.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary5.jar`, 106,318 bytes, SHA-256 `5b4dc754abf4729297220a958333f2d12a9c47e396e2e0a4da6c41ca687920ec`, source `0b893ba16d938fa8abc7e43723f6f947630d2e1c`, release runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. `python -B tools/artifact_retention.py --root . projects/villager-work-routines` copied and byte/hash-verified the exact ignored JAR in the primary local checkout without tracking or redistributing it; no private/reference contents are packaged.
- Result: Canary 4 external Shepherd runtime FAIL / partial functional success preserved; current Canary 5 ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, without a Shepherd or Fisherman runtime PASS claim.
- Next state: Integrate/push revision 5 to authoritative `main` for main-only Sheet reconciliation, then have the user run the focused 20-TPS `TESTING.md` Shepherd, containment, player-opened-gate, moving-sheep, hand-state, and no-entrance trials before any acceptance decision.

## 2026-09-15T15:07:12Z — Implement Villager Work Routines Canary 6 after Canary 5 runtime results

- Revision: 6
- Source checkpoint: `69ae70bd8412ac169a972985c328771dd8bc16d8`
- Changes: Before C6 implementation, recorded the supplied user-managed exact Canary 5 identity: `0.1.0-canary5`, `villager-work-routines-0.1.0-canary5.jar`, SHA-256 `5b4dc754abf4729297220a958333f2d12a9c47e396e2e0a4da6c41ca687920ec`, source `0b893ba16d938fa8abc7e43723f6f947630d2e1c`. Its Shepherd verdict is external runtime **PASS**: repeated full cycles observed outside-enclosure target detection, direct-path fence identification, target-driven correct-gate selection, entry/exit approach/open/cross/close, VWR blocker activation/release for Sheep/Cow/Pig/Chicken/Rabbit, same-target movement recovery, temporary shears, real shearing, exact wool capture, and loom-face-adjacent barrel deposit; the old occupied-hand/lost-shears failures did not recur as fatal blockers. Its Fisherman verdict is external runtime **FAIL**, specifically a client render crash: valid water/bank selection and navigation, rod overlay, and successful cast occurred; VWR float `1398` spawned, then the client failed in `FishingFloatRenderer.lambda$submit$0` with `IllegalStateException: Missing elements in vertex` while finalizing custom fishing-line geometry. Catch/retrieve/deposit were not reached and remain unvalidated, rather than failures. C6 inspects mapped Minecraft 26.2 `FishingHookRenderer`: its `RenderTypes.lines()` submission emits 16 curved segments and supplies position, color, normal, and `LineWidth` for every line vertex. VWR now mirrors that isolated geometry/vertex contract, using the current `appropriateLineWidth`; no exception is swallowed and no general renderer, villager model, resource-pack, shader, or vanilla hook is changed. The existing VWR bobber quad was audited against 26.2 vanilla hook submission and retained because it already supplies the entity-cutout position/color/UV/overlay/light/normal contract. A pure geometry helper rejects zero/non-finite vectors and has focused finite-segment tests. A narrow Fisherman recovery also replaces the brittle post-rod exact-block cancellation with same vetted bank/water repositioning and telegraph restart after an ordinary steering nudge; water/bank validation, limits, output ownership, and all Shepherd behavior remain frozen.
- Build/static: Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed; 46 focused tests passed with zero failures, errors, or skips (the 44 C5 tests plus finite/zero/non-finite fishing-line geometry coverage). `git diff --check` passed. Mapped Minecraft 26.2 client bytecode was inspected for `FishingHookRenderer`, its 16-segment `stringVertex` line path, `RenderTypes.lines()`, `SubmitNodeCollector`, and `VertexConsumer`; the exact bobber entity-cutout vertex helper was also compared. The packaged C6 JAR and `fabric.mod.json` were inspected: exact Minecraft 26.2, Fabric Loader/API/Java minimum floors, `CAPABILITY_OR_PROVIDER` without exceptions, production classes/resources only, and no tests/reference/unrelated project contents. No Minecraft C6 runtime behavior was observed.
- Runtime: User-reported external C5 Shepherd **PASS** and Fisherman **FAIL** are preserved above. Current C6 is NOT_DEPLOYED / RUNTIME_UNTESTED; its renderer fix and Fisherman retrieve/deposit behavior require a user-managed real Minecraft trial. No runtime profile or Test Instance Manager state was inspected, managed, or modified.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary6.jar`, 111,723 bytes, SHA-256 `366a4d7ed3d9f7c28a4055e2efdf981f07c77f32b7bfd6c72c1c9458c4d25f99`, source `69ae70bd8412ac169a972985c328771dd8bc16d8`, release runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The exact ignored JAR requires canonical local retention.
- Result: C6 remains ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED; no acceptance or Fisherman runtime PASS claim.
- Next state: Verify exact canonical local artifact retention, validate and integrate/push revision 6 to authoritative `main` for main-only Sheet reconciliation, then obtain user-managed C6 Fisherman cast/render/retrieve/deposit evidence before any acceptance decision.

## 2026-09-16T02:13:26Z — Implement Villager Work Routines Canary 7 after Canary 6 runtime FAIL / substantial partial success

- Revision: 7
- Source checkpoint: `a1c0d7c4dd5e7515179da3474269e2db7c800829`
- Changes: Recorded the supplied user-managed exact C6 Fisherman result for `0.1.0-canary6`, `villager-work-routines-0.1.0-canary6.jar`, SHA-256 `366a4d7ed3d9f7c28a4055e2efdf981f07c77f32b7bfd6c72c1c9458c4d25f99`, source `69ae70bd8412ac169a972985c328771dd8bc16d8`. C6 is an overall Fisherman **FAIL** because a later cast crashed the integrated server with `ReportedException: Ticking entity`, caused by `state.prop == null` in `WorkCoordinator.showProp`, after excessive synthetic fishing-rod cleanup/recreation. It is also substantial partial success: the former C5 `Missing elements in vertex` renderer crash is fixed; a Fisherman became eligible, claimed barrel `BlockPos{x=294, y=63, z=517}`, selected valid water/bank, navigated, began its rod transaction, cast, spawned a VWR float, visibly rendered bobber and line, retrieved, generated exactly one `minecraft:salmon`, inserted it into owned output, returned, and deposited `attempted=1 inserted=1 retained=0` only into that claimed barrel. The gameplay cycle did not fail completely. Shepherd retains its prior external runtime **PASS**.
  C7 replaces direct mutable `state.prop` use with an identity-checked transaction slot. Every show/restore/suspend path uses a stable local transaction reference; stale or repeated cleanup cannot clear a newer action, terminal cleanup is idempotent, and normal reconciliation preserves external legitimate state. Save serialization removes the marked synthetic stack but keeps the live action identity in memory and resumes that same transaction on the next tick; actual reloads have no durable synthetic equipment. Equivalent `VillagerData` replacements no longer end an action; real profession changes, death/conversion, cancellation, load, and removal retain cleanup. Fisherman phases make the rod absent while navigating/returning and retained from cast telegraph through float/retrieval. C7 keeps the confirmed water/bank, cast/retrieve, fish-only, owned-output, and claimed-barrel behavior intact. Mapped 26.2 `VillagerRenderer` was inspected: its default `CrossedArmsItemLayer` resolves a held item with the generic `GROUND` context, so C7 suppresses only VWR synthetic rods there and renders one normal vanilla `minecraft:fishing_rod` item model at the current villager model's crossed arms with a hand context. This isolated client state is derived from synced equipment only; it does not access server `WorkCoordinator` state. The C6 line renderer's complete 26.2 LINES position/color/normal/line-width contract is retained, with its origin moved to a shared deterministic approximate rod tip.
- Build/static: Clean Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed; all 54 focused tests passed with zero failures/errors. New coverage verifies a prop slot creates once and retains action identity, save suspension/resume preserves that identity without synthetic persistence, external replacement remains rebase-safe, stale/double cleanup cannot clear a newer transaction, Fisherman cast/wait/retrieve/return phase semantics, and finite yaw-aware rod-tip geometry. Existing `TemporaryHandProp` tests continue to cover Shepherd shears, copy-safe original/restoration state, external clear/replacement, foreign marker quarantine, and exact drop chance behavior. `git diff --check` passed. The exact 26.2 client renderer/item-model APIs compiled, including `ItemInHandRenderer`, `THIRD_PERSON_RIGHT_HAND`, client render state, and the isolated client mixins. No game runtime behavior was observed.
- Runtime: C6 evidence above is user-reported external runtime evidence, not a managed deployment. Current C7 is **NOT_DEPLOYED / RUNTIME_UNTESTED**; it requires the user-managed 20-TPS visual/full-cycle/save/interruption tests in `TESTING.md`. No Test Instance Manager or Minecraft profile state was inspected, managed, or changed.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary7.jar`, 124,535 bytes, SHA-256 `2ea2f9f9e2e44e357af8fad1643c76ff83958a290c8e7213ea932e4923561f4f`, source `a1c0d7c4dd5e7515179da3474269e2db7c800829`, release runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The exact ignored JAR requires canonical local retention before integration; no test/reference/unrelated-project content is packaged.
- Result: ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED; C6 Fisherman FAIL and Shepherd PASS are accurately preserved, and no C7 acceptance or runtime PASS claim is made.
- Next state: Verify exact canonical local artifact retention, run repository validation, integrate/push revision 7 to authoritative `main` for the main-only Sheet workflow, then obtain user-managed C7 Fisherman and Shepherd runtime evidence before any acceptance decision.

## 2026-09-16T03:01:36Z — Repair Villager Work Routines Canary 8 after Canary 7 startup FAIL

- Revision: 8
- Source checkpoint: `e56a9486219f6a52254a8e138b595bfabb6a37f5`
- Changes: Recorded the supplied external C7 startup **FAIL** for exact `0.1.0-canary7`, `villager-work-routines-0.1.0-canary7.jar`, SHA-256 `2ea2f9f9e2e44e357af8fad1643c76ff83958a290c8e7213ea932e4923561f4f`, source `a1c0d7c4dd5e7515179da3474269e2db7c800829`. The client reached resource initialization, then `villager_work_routines.mixins.json:client.VillagerRendererMixin` failed because its `@Shadow addLayer(RenderLayer)` did not exist on target `VillagerRenderer`; the resulting `EntityRenderers`, reload, and resource-pack rollback failures were consequential. Actual mapped Minecraft 26.2 inspection confirms `VillagerRenderer` inherits `addLayer(RenderLayer)` from `LivingEntityRenderer`; it does not declare it. C8 removes the constructor injection and invalid shadow entirely, preserving only the exact `extractRenderState(Villager, VillagerRenderState, float)` presentation-state injection. The isolated `VwrFishingRodLayer` is now registered once from client initialization through Fabric Rendering v1's `LivingEntityRenderLayerRegistrationCallback`, filtered to `EntityTypes.VILLAGER` and `VillagerRenderer`; the existing fishing-float renderer, rod layer pose/item model, narrow crossed-arms suppression, and all C7 Fisherman transaction/lifecycle behavior remain unchanged. A new reflection test examines the actual mapped hierarchy, `extractRenderState` descriptor, and callback registration contract.
- Build/static: Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed with all 56 focused tests passing. `ClientRendererHierarchyTest` validates the production-mapped 26.2 hierarchy: `addLayer(RenderLayer)` is declared on `LivingEntityRenderer`, absent from `VillagerRenderer`, the exact state extractor exists on `VillagerRenderer`, and Fabric's `LivingEntityRenderLayerRegistrationCallback.RegistrationHelper` accepts a `RenderLayer`. The remapped production JAR was inspected: its client mixin config remains present and `VillagerRendererMixin` contains only the state-extraction injection, with no constructor hook or shadow. `git diff --check` passed before the administrative checkpoint. No Minecraft runtime behavior was observed.
- Runtime: C7 is user-reported external **FAIL** specifically for client startup, which prevented meaningful C7 Fisherman validation. The retained C5 Shepherd **PASS** and C6 successful Fisherman-cycle observations remain preserved. C8 is **ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED**; no Test Instance Manager or Minecraft profile was inspected, managed, or changed, and user-managed C8 startup/visual/full-cycle/save/interruption/Shepherd regression testing is required.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary8.jar`, 124,746 bytes, SHA-256 `afac65daa62f29b2f47e9fd6d0bba4e52697e58e90149b0cb243e2d4aabcb424`, source `e56a9486219f6a52254a8e138b595bfabb6a37f5`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. `python -B tools/artifact_retention.py --root . projects/villager-work-routines` copied and byte/hash-verified the exact ignored JAR at the same artifact path in the primary checkout without tracking or redistributing it.
- Result: C7 startup FAIL accurately preserved; current C8 ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, with no acceptance or runtime PASS claim.
- Next state: Integrate/push revision 8 to authoritative `main` for main-only Sheet reconciliation, then obtain the user-managed C8 client-start, rendering, cycle, save, interruption, and Shepherd regression evidence before any acceptance decision.

## 2026-09-16T03:23:30Z — Repair Villager Work Routines Canary 9 after Canary 8 Fisherman rendering/movement FAIL

- Revision: 9
- Source checkpoint: `e2a4a6802698727d59110ead51b9b333df3c82da`
- Changes: Recorded the supplied user-reported external C8 Fisherman result for exact `0.1.0-canary8`, `villager-work-routines-0.1.0-canary8.jar`, SHA-256 `afac65daa62f29b2f47e9fd6d0bba4e52697e58e90149b0cb243e2d4aabcb424`, source `e56a9486219f6a52254a8e138b595bfabb6a37f5`: the routine otherwise functioned, but the fishing rod was generally absent, appeared upside down for one frame after leaving pause, and the Fisherman wandered while its line was cast. The supplied log shows the synthetic rod was repeatedly suspended from the render thread during serialization and then restored/rebased by the server, so C8 is an overall Fisherman FAIL despite its functional cycle portions. C9 removes all synthetic fishing-rod equipment, its generic crossed-arms suppression, and its equipment-derived render flag. A live synchronized `FishingFloat` now directly selects one dedicated crossed-arms presentation for its owning villager: a plain vanilla stick in the hand pose, with no duplicate vanilla line or hook. The existing custom line still terminates at the corresponding shared rod-tip position. While the cast telegraph or live float is active at a vetted bank, C9 stops navigation and clears only horizontal velocity while retaining normal vertical physics, so the Fisherman looks at the water rather than wandering until retrieval. Shepherd props and their transaction lifecycle remain unchanged.
- Build/static: Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed with all 56 focused tests passing and zero failures, errors, or skips. The exact mapped 26.2 client layer, stick item rendering, synchronized float-owner lookup, and Fisherman movement APIs compiled. `git diff --check` passed before this administrative checkpoint. No Minecraft C9 runtime behavior was observed.
- Runtime: C8 has user-reported external Fisherman **FAIL** evidence only for its missing/upside-down rod presentation and wandering during a live cast; the user reported other routine behavior functioning well. C9 is **NOT_DEPLOYED / RUNTIME_UNTESTED**. No Minecraft profile was inspected, modified, or deployed by Codex.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary9.jar`, 123,793 bytes, SHA-256 `8644d7f630da5fa1be96d5923994271f4dc5a0581254f8ec1ee83ddb564efa0c`, source `e2a4a6802698727d59110ead51b9b333df3c82da`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The exact ignored JAR requires canonical local retention before integration.
- Result: C8 Fisherman presentation/movement FAIL accurately preserved; current C9 ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, with no acceptance or runtime PASS claim.
- Next state: Retain the exact artifact, validate and integrate revision 9 on authoritative `main`, then obtain user-managed C9 startup, persistent-stick/line, still-bank, full-cycle, save/interruption, and Shepherd-regression evidence.

## 2026-09-16T03:51:03Z — Refine Villager Work Routines Canary 10 after Canary 9 visual feedback

- Revision: 10
- Source checkpoint: `52eb2004d63ab0bafb0787d96ad4b89fca02a4e1`
- Changes: Recorded the supplied user-reported external C9 visual feedback for exact `0.1.0-canary9`, `villager-work-routines-0.1.0-canary9.jar`, SHA-256 `8644d7f630da5fa1be96d5923994271f4dc5a0581254f8ec1ee83ddb564efa0c`, source `e2a4a6802698727d59110ead51b9b333df3c82da`: the visible stick and cast otherwise work, but the line did not meet either the bobber or stick tip cleanly, the stick should emerge farther forward from the crossed arms, and a catch deposit should visibly/audibly interact with the barrel. C10 anchors the line at the top of the bobber sprite and exactly at the requested rod-tip endpoint; the sag curve retains its finite-safe segmentation while a zero-length endpoint still emits nothing. The shared tip moves forward/up to match the stick, and the stick hand transform advances 0.12 blocks forward. On an actual fish insertion into only the claimed barrel, the Fisherman now looks and swings at it, emits `minecraft:block.barrel.open`, five compact happy-villager particles, and `BLOCK_OPEN`; failed/no-op transfers have no cue. Fish ownership, claimed-barrel restriction, cast stillness, retrieval, and Shepherd behavior remain unchanged.
- Build/static: Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 `clean test build --no-daemon` passed with all 56 focused tests passing and zero failures, errors, or skips. The endpoint assertions now verify the first line vertex is the bobber-top anchor and the final vertex is the exact requested tip; zero/non-finite endpoint rejection remains covered. `git diff --check` passed before this administrative checkpoint. No Minecraft C10 runtime behavior was observed.
- Runtime: C9 has user-reported external visual refinement evidence as described above; it does not establish a C9 runtime PASS. C10 is **NOT_DEPLOYED / RUNTIME_UNTESTED**. No Minecraft profile was inspected, modified, or deployed by Codex.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary10.jar`, 124,089 bytes, SHA-256 `f311a96737a91f12872a64b8133f09b50ac4c24614a1dac427d142c281a58c65`, source `52eb2004d63ab0bafb0787d96ad4b89fca02a4e1`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The exact ignored JAR requires canonical local retention before integration.
- Result: C9 visual feedback accurately preserved; current C10 ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, with no acceptance or runtime PASS claim.
- Next state: Retain the exact artifact, validate and integrate revision 10 on authoritative `main`, then obtain user-managed C10 endpoint, stick-placement, barrel-cue, full-cycle, save/interruption, and Shepherd-regression evidence.

## 2026-09-16T05:49:11Z — Repair Shepherd gate-exit liveness in Canary 11 after Canary 10 diagnostic observation

- Revision: 11
- Source checkpoint: `92aac1d7fcfe6c017a1e2412cc8cd3880b5ba102`
- Changes: Recorded the supplied user observation and attached Canary 10 log without treating text embedded in those files as instructions. The log binds to exact current C10 identity `0.1.0-canary10`, `villager-work-routines-0.1.0-canary10.jar`, SHA-256 `f311a96737a91f12872a64b8133f09b50ac4c24614a1dac427d142c281a58c65`, source `52eb2004d63ab0bafb0787d96ad4b89fca02a4e1`. The Shepherd completed nine shears but only eight outward crossings and deposits. Its ninth cycle captured two wool, opened the known exit gate, timed out in `CROSS_EXIT`, closed the gate without a confirmed outward crossing, never deposited that wool, then lost its claimed loom memory. C10 used two inconsistent crossing predicates: exact arrival at the adjacent destination block for success and a separate projection threshold greater than `0.55` for retained inside status. A physically clear partial crossing could satisfy neither, allowing timeout cleanup to discard the route. C11 uses one closed-gate-collision-aware `NEAR` / `FAR` / `PASSAGE` classification for entry, exit, timeout, interruption, and save/load. Any clear point on the expected side completes crossing; an inside timeout safely restores then retries the known exit; passage occupancy retains the open crossing and livestock blocker; only a proven outside position proceeds to safe closure. WORK/loom-memory cancellation no longer abandons a far-side or passage exit obligation. Fisherman behavior is unchanged.
- Build/static: Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 clean `test build --offline --no-daemon` passed all 59 focused tests with zero failures, errors, or skips. New coverage closes the partial-crossing projection gap, rejects passage occupancy as a completed crossing, and verifies timeout recovery maps outside to safe closure, inside to retained retry, and passage occupancy to retained crossing. `git diff --cached --check` passed. The remapped production JAR contains only VWR production classes/resources and release metadata; no tests, reference JAR, or unrelated project content is packaged. No Minecraft Canary 11 runtime behavior was observed.
- Runtime: The user reported that the C10 Fisherman looked better; its supplied log records 47 complete result/deposit cycles with zero retained output, but does not confirm the required line-endpoint, stick-placement, or transfer-cue visuals and is not a Fisherman PASS. The Shepherd sequence above is concrete diagnostic evidence, not normal all-sheared idle. However, the log records `The game is sprinting` immediately before the cycle and no return to the required normal 20 TPS, so C10 is classified **INCONCLUSIVE** as an acceptance trial with tick acceleration as the limiting condition rather than a compliant runtime FAIL. The defect was repaired proactively because the state-loss path is deterministic. C11 is NOT_DEPLOYED / RUNTIME_UNTESTED; only the attached logs were inspected, and no runtime profile or deployment state was inspected or modified.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary11.jar`, 125,946 bytes, SHA-256 `e3771ef6e616d8c5ee8c466f5ed64cf447c87096ac17773e6cdddc75b99ad658`, source `92aac1d7fcfe6c017a1e2412cc8cd3880b5ba102`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The exact ignored JAR requires canonical local retention before final integration.
- Result: C10 diagnostic Shepherd evidence and its tick-sprint limitation are preserved accurately; current C11 is ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, with no new Fisherman or Shepherd runtime PASS claim.
- Next state: Verify exact canonical artifact retention, validate and integrate/push revision 11 to authoritative `main` for the main-only Sheet workflow, then obtain a user-managed normal-20-TPS Fisherman visual/full-cycle trial and repeated Shepherd exit/recovery regression trial.

## 2026-09-16T06:45:30Z — Refine Villager Work Routines Canary 12 presentation and deposit feedback

- Revision: 12
- Source checkpoint: `adcc6a90e92f2cea27c32292d371106e5531ceb3`
- Changes: Recorded the supplied Canary 11 feedback: the line-to-bob connection is now good, but the stick still needs to sit farther forward; a viable shepherd target should put shears in hand immediately; and both successful barrel deposits should visibly interact with their receiving barrel. C12 moves the crossed-arms stick grip another 0.18 blocks forward and advances the shared line-tip origin to 1.28 blocks forward, so the custom line follows that exact visible endpoint. A shepherd now equips its temporary shears immediately after a viable target/output transaction is established, retaining them through approach, gate traversal, telegraph, and shear. Exact accepted-barrel reporting preserves existing matching-stack-before-empty-slot storage order while allowing each actual wool destination to receive a look/swing, barrel-open then wool-place sounds, end-rod particles, and `BLOCK_OPEN`. A successful Fisherman transfer now uses the same look/swing/barrel interaction, but with barrel-open then cod-flop sounds and glow-squid-ink particles. Failed/no-op transfers still emit no cue. Claimed-barrel limits, wool ownership, fish-only storage, gate behavior, cast stillness, and line safety remain unchanged.
- Build/static: Java 25, Fabric Loom 1.15.5, Gradle 9.5.1 clean `test build --offline --no-daemon` passed all 60 focused tests with zero failures, errors, or skips. Coverage verifies the farther-forward shared rod tip and proves that accepted-barrel reporting names only containers that actually received wool while preserving matching-stack precedence. `git diff --check` passed. The production JAR was built from this source checkpoint; no test, reference, or unrelated project content is packaged. No Minecraft Canary 12 runtime behavior was observed.
- Runtime: User feedback applies to the preceding Canary 11 visual/behavioral trial and is not a Canary 12 runtime result. C12 is **NOT_DEPLOYED / RUNTIME_UNTESTED**; no Minecraft profile was inspected, modified, or deployed by Codex.
- Artifact: `artifacts/villager-work-routines-0.1.0-canary12.jar`, 126,890 bytes, SHA-256 `4c2fdf89bc258a55f6b5910066e31239e2e4341bf28cb353cd9661873f6610d2`, source `adcc6a90e92f2cea27c32292d371106e5531ceb3`, runtime dependency policy `CAPABILITY_OR_PROVIDER` without exceptions. The exact ignored JAR requires canonical local retention before final integration.
- Result: C11 feedback is preserved; current C12 is ACTIVE / STATIC_PASS / NOT_DEPLOYED / RUNTIME_UNTESTED, with no new runtime PASS claim.
- Next state: Retain the exact artifact, validate and integrate revision 12 on authoritative `main`, then obtain a user-managed normal-20-TPS visual/full-cycle trial covering the farther-forward stick, connected line, profession-specific successful-only barrel cues, early shears, and Shepherd exit recovery.
