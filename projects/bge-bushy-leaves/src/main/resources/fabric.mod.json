{
  "schemaVersion": 1,
  "id": "bge_bushy_leaves",
  "version": "${version}",
  "name": "BGE × Bushy Leaves",
  "description": "Adds authored, patch-local foliage using the active canonical leaf appearance and BGE-authoritative exposed surfaces.",
  "authors": ["Resivore"],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": ["dev.resivore.bgebushyleaves.client.BgeBushyLeavesClient"]
  },
  "depends": {
    "fabricloader": ">=0.19.3",
    "fabric-api": ">=0.156.0",
    "minecraft": "~26.2",
    "java": ">=25",
    "cnm_terrain_slabs_compat": ">=4.2.22-bge.canary78.stair-wall-surface-authority+26.2"
  },
  "custom": {
    "runtime_dependency_policy": {
      "contract": "CAPABILITY_OR_PROVIDER",
      "exceptions": [
        {
          "consumer_id": "bge_bushy_leaves",
          "relationship": "depends",
          "dependency_id": "minecraft",
          "predicate": "~26.2",
          "reason": "Canary 5 consumes Minecraft 26.2's public final model, renderer-quad, and BGE surface-contract seams while retaining only BGE's oldest required public SurfacePatch capability.",
          "regression_evidence": [
            "Controlled verification resolves, SHA-256 verifies, and compiles against the BGE artifact currently declared by authoritative main; this is build evidence, not an exact runtime BGE dependency."
          ]
        }
      ]
    },
    "workbench:classification": "GENERATED / CONTROLLED VALIDATION / RUNTIME UNTESTED",
    "workbench:deployment": "NOT DEPLOYED"
  }
}
