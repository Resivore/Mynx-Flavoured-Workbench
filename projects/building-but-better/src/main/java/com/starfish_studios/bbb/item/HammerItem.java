package com.starfish_studios.bbb.item;

import net.minecraft.world.item.Item;

/** The retained 2.0pre4 Hammer. Layer-specific durability loss is intentionally absent. */
public final class HammerItem extends Item {
    public HammerItem(Properties properties) {
        super(properties);
    }
}
