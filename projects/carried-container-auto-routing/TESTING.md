# Testing

**ACTIVE — CONTROLLED VALIDATION PASS — NOT DEPLOYED — RUNTIME UNTESTED**

Current CCAR is `carried-container-auto-routing-0.3.15-shulker-pick-block-canary1.jar`, SHA-256 `340b3cd2bfcb977c00cc06071893f9627ba0c32b26325d381a62922000561b3c`, source `dbb8f82a260affff8884dd47628169bb4eb1be7a`; pair it with CSR Canary 19. Test only under explicit Test Instance Manager ownership in the dedicated Matcha Flavoured 26.2 Workbench. Never open or alter the protected Matcha Flavoured 26.1.2 profile.

1. In Survival, Pick Block an item already in the selected hotbar, another hotbar slot, and ordinary storage. Confirm vanilla selection/swap behavior is unchanged and every carried shulker remains byte-for-byte unchanged.
2. Remove all ordinary matches and place differently sized exact-component source stacks in multiple unlocked carried shulkers and physical slots. Confirm the first CCAR carrier and lowest physical matching slot win, the entire source stack moves, and vanilla's suitable empty/non-enchanted hotbar choice is preserved.
3. Repeat with the only match in a locked shulker, a bundle, an unsupported carrier, and a bundle nested as a direct shulker item. Confirm Pick Block remains a no-match and nothing is exposed or mutated. Repeat in Creative and confirm vanilla Creative behavior is unchanged.
4. With a filled suitable hotbar destination, displace a component-rich stack into: one ordinary compatible partial; one unlocked-shulker compatible partial; and several partials spanning multiple hosts. Confirm CCAR order, exact components/counts, effective non-64 maxima, lock state, nesting denial, and CSR admission on every allocation.
5. Leave only partial compatible capacity plus one empty ordinary slot. Confirm partials fill first and the exact remainder enters the first empty player-inventory slot in live vanilla index order. Repeat with Inventory Extended and verify its first and last live storage positions are included.
6. Remove all compatible capacity while retaining an empty ordinary slot. Confirm the displaced stack enters that ordinary slot and the physical shulker source slot becomes empty; no arbitrary empty shulker slot may be used.
7. Fill every ordinary slot and remove compatible capacity. Confirm the displaced stack enters the exact source shulker slot, not another slot. Repeat with partial compatible capacity and confirm only the remainder uses that exact source slot.
8. Reserve the source slot for the requested item in CSR, fill ordinary storage, and use an incompatible displaced item. Confirm the entire Pick Block transaction fails: source, selected stack, reservation, portable identity, and locks remain unchanged. Then use a matching reservation and confirm legal admission succeeds without rewriting reservation data.
9. During multiplayer-oriented stress, change the physical source, selected stack, carrier, or selected index between request and synchronization. Stop on any duplication, loss, partial commit, stale overwrite, component loss, lock mutation, or reservation mutation.
10. Recheck passive pickup, external QUICK_MOVE, player-origin QUICK_MOVE, Offhand Shift-Click composition, QSN, Inventory Extended, Stacks Are Stacks, bundle behavior, routed-pickup audio, CSR panel lock control/art, keyboard lock control, and save/reload. Stop on any regression and record only behavior actually observed at runtime.
