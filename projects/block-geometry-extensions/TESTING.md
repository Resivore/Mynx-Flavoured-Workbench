# Testing

## Candidate and evidence boundary

Exact BGE C55 is installed and readiness-verified in Slot A together with the unchanged exact Nibaru C46 contract artifact:

- `artifacts/cnm-nibaru-integration-0.7.0-bge-canary55-corner-column.jar`, 359,920 bytes, SHA-256 `1FC39A43AD9DB36DBA6F26145A080B7F0D13FBA3B0663D521E1619496F3767BE`;
- `../nibaru/artifacts/more-slabs-stairs-and-walls-4.2.0+26.2-port-canary46-bge-layer-contract.jar`, 5,652,769 bytes, SHA-256 `3281D110F062DB62E721D838A35CE915CA73DD41AF098A013B52952561CEAE7D`.

BGE C55 was built from source `09b2bc10763363adb8cd0693a4ae821c67f36f26`. The Java 25 clean build, 63 static architecture/resource checks, and all 80 controlled GameTests passed. Manager revision 41 returned `READY_TO_TEST_VERIFIED / UNTESTED`; Minecraft was not launched for C55. Static checks, GameTests, artifact identity, and manager readiness are not a runtime pass.

Predecessor C54 is `FAIL` only for the user's reported off-center Layer item presentation. The user reported that C54 with the current Shulker Trowel integration otherwise looked good, but no individual unreported matrix row is inferred. C55 applies an item-only shift of exactly 3 pixels right and 6 pixels down relative to C54; placed-world Layer geometry is unchanged. Accepted/rollback BGE C52 remains unchanged, and neither C54 nor C55 is promoted.

## C55 focused runtime matrix

1. Confirm manager revision 41 reports Slot A `READY_TO_TEST_VERIFIED / UNTESTED` with the exact C55 and C46 identities above and unchanged Shulker Trowel C6 in Slot B. Launch only the dedicated Minecraft 26.2 Workbench under explicit runtime ownership; never use the protected 26.1.2 profile. Stop on a Loader rejection, startup crash, registry/mixin error, ShapeMap error, missing resource, or other relevant log error.
2. Compare representative ordinary, pillar/axis, and Glazed Layer items against C54 intent. Confirm the C55 Layer item is exactly 3 pixels farther right and 6 pixels farther down, is centered consistently in inventory/hotbar, and has no placed-world Layer geometry, material, pattern, or UV regression.
3. In survival, test the shared compound-geometry economy directly with Layer and Quarter Column items. For each geometry, confirm the first placement into an empty block position consumes one item; every compatible in-place expansion consumes none; full, failed, wrong-face, and incompatible attempts consume none; and normal breaking returns exactly one canonical source block. Block adjacent valid placement while testing intended failures.
4. Recheck Layer in all six directions: opposite-face anchoring, 4/8/12/16-pixel visible and collision thickness, exposed-face-only compatible growth, waterlogging for partial states, dry/full behavior, deterministic ShapeMap order, no generated-family recursion, and unchanged Step/Vertical world behavior.
5. Exercise Corner's four horizontal orientations in both bottom and top halves. Confirm each state is exactly 16×8×8 pixels, rotates/mirrors and collides correctly, does not compose in-place, preserves material identity, and returns one canonical source block.
6. Exercise Quarter Column singleton quadrants `NW`, `NE`, `SW`, and `SE`, each exactly 8×16×8 pixels. Confirm only these horizontal growth pairs: `NW` east/south and `SE` west/north become `NW_SE`; `NE` west/south and `SW` east/north become `NE_SW`. Both diagonal doubles are terminal; vertical, wrong-side, full, and incompatible attempts do not mutate state or consume items. Check waterlogging and collision for singletons and doubles.
7. For Grass Block Column, confirm every upward-facing horizontal surface uses canonical TOP, every downward-facing horizontal surface uses canonical BOTTOM, every vertical surface uses canonical SIDE, and SIDE UVs remain upright. Geometry orientation must not rotate the material coordinate frame.
8. Sample all canonical pillar policies across X/Y/Z: Crimson Stem (`STANDARD_ROTATED`), Oak Log (`HORIZONTAL_ROTATED`), and Bamboo Block (`DIRECT_UV_LOCKED`). Confirm geometry reconstructs in the intended world position while authored end/side roles and UV rotations remain canonical. Also sample Glazed/pattern orientation independently from physical geometry.
9. Smoke-check overlay/tint, leaves/cutout, glass/translucency, path and spreadable transitions, falling/concrete behavior, copper/weathering, coral, redstone, magma, soul sand, honey/slime, ice/water, fuel, stripping, podzol, typed DROP_BASE transitions, and one-source drops on representative Corner and Column materials. Record only behavior actually observed.
10. Stop and leave C55 unaccepted on any incorrect identity, item offset, geometry, placement/growth mapping, economy, drop, collision, water state, material face/UV orientation, ShapeMap order, duplicate/recursion, specialized smoke behavior, Step/Vertical/Layer regression, or relevant log error. Record each actual runtime observation as `PASS`, `FAIL`, or `INCONCLUSIVE`; do not infer untested rows.
