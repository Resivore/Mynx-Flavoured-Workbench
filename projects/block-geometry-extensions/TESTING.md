# BGE C90 CNM resolved-Wall supersession manual verification

Current candidate: `BGE C90.jar`

- Embedded version: `4.2.34-bge.canary90.cnm-resolved-wall-supersession+26.2`
- SHA-256: `1f58de8459ec1312633701e2b86170d529119d0cdb01662fd07bdcddb6c91e1d`
- Source checkpoint: `05c2d78aa4356906e252d3af2d825d7c6184146e`
- Lifecycle/evidence: `ACTIVE / CONTROLLED_VALIDATION_PASS / RUNTIME_UNTESTED`
- Immediate predecessor and supplied runtime-failure evidence: exact C89 `BGE C89.jar`, SHA-256 `cc1368f48fffcde03997c4d3f1d0e8907236c584d7ae107f35eb4e06e98667f2`. Its reported `BGE canonical binding validation failed` world-load abort applies only to those exact C89 bytes.
- Rollback: exact C72 `cnm-nibaru-integration-4.2.16-bge.canary72.farmland-slab-low-water+26.2.jar`, SHA-256 `f9f892fccbbee85f75f03c9b24752bbeab76f4fa60efd867414969b735ae85a3`.
- Accepted release: exact C70 `cnm-nibaru-integration-4.2.14-bge.canary70.stone-native-slab+26.2.jar`, SHA-256 `d304552e29e76c4165675415215439ac2d73b5a6ebc4abc9787f0fa1124cf266`.

This checklist is lifecycle-neutral. Record only behavior observed for these exact C90
bytes. It does not authorize inspecting, creating, selecting, populating, or modifying
any protected or retired Minecraft profile.

## Resolved real-Wall lifecycle

1. In an ordinary permitted environment with stock Clutter No More 2.0.7+26.2, use an untyped CNM slab admission whose resolved ShapeMap component has a real provider Wall. Complete normal initial loading, then one ordinary ShapeMap rebuild (for example, a legitimate resource reload), without changing the block set between passes.
2. Confirm the family has exactly its real provider Wall plus one BGE Corner, Quarter Column, and Layer. The preregistered deferred `*_slab_wall` carrier must not appear in the resolved component, a selector, generated resources, or as another material root.
3. World loading and the rebuilt pass must complete without `BGE canonical binding validation failed`. If diagnostics are available, confirm the superseded carrier is explicitly documented as exempt rather than assigned a synthetic canonical binding.
4. Confirm no `material_slab_wall`, `material_wall_slab`, or other geometry-from-geometry family appears. This remains a generic role/component rule, not a material-, namespace-, provider-, or manual Enderscape-compatibility procedure.

## Resolved wall-free lifecycle

1. Repeat with a legitimate untyped CNM slab admission whose resolved component has no real Wall.
2. Confirm the one preregistered deferred Wall becomes that resolved family's canonical Wall, and the same family contains exactly one BGE Corner, Quarter Column, and Layer. No duplicate Wall or recursive family may appear after the rebuild.

## Retained C87/C88/C89 behavior

1. Across first load and one legitimate ShapeMap reconstruction, normal direct material family autopopulation still produces its supported existing CNM slab, stair, wall, and BGE roles exactly once. C90 must not remove ordinary CNM completion, alter canonical-parent selection, or weaken canonical-binding validation.
2. Before a manual reload, open or place representative Building But Better standard beam stairs and BGE-generated forms. There must be no missing canonical model/resource exception; repeat after the rebuild.
3. Confirm that any canonically bound non-root geometry remains ineligible to recursively seed a later CNM family, preserving C89's `material_slab_wall` / `material_wall_slab` guard.

## Evidence and retention

1. For every observation, record the exact candidate SHA-256, source/mod versions, datapack configuration, repeatable sequence, expected result, and observed result. Controlled builds, static checks, and GameTests do not establish gameplay acceptance.
2. Retain `BGE C90.jar` by its exact SHA-256 before runtime work. Preserve C89 as failed predecessor provenance, C72 as rollback provenance, and C70 as the accepted release; do not overwrite retained artifacts.
