{
  "schemaVersion": 1,
  "id": "bge_client_gametest",
  "version": "${version}",
  "name": "BGE Client Lookup Tests",
  "environment": "client",
  "entrypoints": {
    "fabric-client-gametest": [
      "dev.aero.cnmterraincompat.SpruceRollbackClientGameTest"
    ]
  },
  "depends": {
    "fabricloader": ">=0.19.3",
    "fabric-api": "*",
    "minecraft": "26.2",
    "cnm_terrain_slabs_compat": "=${version}"
  }
}
